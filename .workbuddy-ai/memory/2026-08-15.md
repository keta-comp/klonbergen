# 2026-08-15

## Hero faqat qayta islendi (to'liq redesign to'xtatildi)
- Foydalanuvchi to'liq sayt redesign ni to'xtatishin aytdi: faqat HERO qismini tuzatish kerak.
- O'zgartirilmagan: `/hero.mp4` (kelin-kuyov video), opening tajribasi (OpeningExperience), boshqa barcha section lar.
- Nav muammosi tuzatildi: fixed + shaffof (video ustida yo'qolib ketgan) -> endi doimiy ivory translucent `rgba(247,242,232,0.9)`, ink matn; hero content `padding-top: calc(var(--nav-h) + ...)` orqali nav ostida, overlap yo'q.
- Hero `min-height:100svh` flex column; content markazda, clamp() o'lchamlar; sarlavha `clamp(2.1rem,8vw,6.2rem)` line-height .92 (oversized emas).
- Overlay yengillashtirildi: `.hero-scrim` og'ir qora gradient o'rniga yengil cinematic gradient (markazda juft ko'rinadi); `.hero-video` `brightness(.72)->.96`, qora filtr olib tashlandi.
- Hero iyerarxiyasi: nav -> "SANLI MIRÁTNAMA" -> "Ómirińizdegi / eń ullı kúnińiz" -> 1 qisqa jumla -> 1 primary CTA + 1 subtle CTA -> scroll cue.
- Qo'shildi: film grain (global), subtle text reveal (framer + useReducedMotion), very slow zoom (`heroZoom` 30s), scroll indicator.
- Tekshirish: Playwright orqali 1440x900, 1920x1080, 1366x768, 390x844 da HAMMASI PASS (hero==100svh, nav overlap yo'q, CTA viewport ichida, horizontal overflow yo'q). `tsc --noEmit` OK.
- Muhim: Vite dev server `/node_modules/.vite` bulk-delete safe-delete shimi tufayli ishga tushmaydi; `rm -rf node_modules/.vite` (bash) bilan tozalanadi. Verify uchun static preview (.hero-preview.html) + scripts/verify-hero.mjs ishlatildi.

## 1:1 redesign (screenshot bo'yicha)
- Foydalanuvchi butun saytni screenshot ga 1:1 qayta ishlashni talab qildi: "Sayt shunaqa bolishi kerak tushwindi mi huddi 1 ga 1 qilib ishlab ber hero video ozgarmaydi faqat 1ga 1 qilib ishla zaybal qilding."
- `src/pages/Index.tsx` to'liq qayta yozildi. 7 ta section: Hero (chapga tekislangan, 2 CTA — gold + outline, pastki hint), QR Experience (4 step horizontal flow + 2 overlapping phone), Features (dark, 6-card grid), Live Toy (5-step horizontal timeline), Sezimleri (dark, 4 unique photo), Venue Panel (telefon admin UI bilan), Pricing (3 plan, featured dark/gold), Footer.
- `src/index.css` ga ~250 qator yangi komponent CSS qo'shildi (`.qr-flow`, `.qr-phone-pair`, `.features-grid`, `.horizontal-timeline`, `.sezimleri-grid`, `.plus-120`, `.admin-screen`, `.vow-price`).
- `/hero.mp4` o'zgarmay qoldi (faqat src/hero-video atribut qo'shildi, fayl identifikatori o'zgarmagan).
- Pricing featured card narxi (399 000) `.vow-display` `color: var(--vow-ink)` qoidasi tufayli ko'rinmas edi. `.vow-price` + `.is-featured` klassi qo'shildi (specificity > `.vow-display`); endi ivory da ko'rinadi.
- Verify: `scripts/verify-sections.mjs` (Playwright, reducedMotion: reduce — OpeningExperience overlay bypass) `.verify-sections/` ga 11 ta per-section screenshot. `tsc --noEmit` OK.

## Qora fonga bo'yalganlarni oq qilish + telefon rasmlari
- Foydalanuvchi 3 ta o'zgarish so'radi:
  1. Qora fonga ega bo'lgan sectionlar rangini oq qilish (Features, Sezimleri, Footer — `bg-[var(--vow-dark)]` → `bg-[var(--vow-ivory)]`).
  2. /public/phone-1.png va /public/phone-2.png rasmlarini QR Experience dagi 2 ta phone mockup o'rniga qo'yish (Vowly Panel rasmi).
  3. Saytdagi matnlarni kichikroq va ixchamroq qilish (`vow-display-md` clamp(2.1, 4.6vw, 3.6rem) → clamp(1.7, 3.4vw, 2.7rem); hero-title clamp(2.1, 8vw, 6.2rem) → clamp(1.7, 6vw, 4.6rem); vow-label 0.7rem → 0.65rem; vow-body clamp(1, 1.15vw, 1.18rem) → clamp(0.86, 0.95vw, 0.98rem)).
- O'zgarishlar:
  - `src/pages/Index.tsx`: dark-bg sections → ivory; QR Experience'da `<PhoneMockup>` (InvitationCard + AppScreenMini) o'rniga `<img src="/phone-2.png">` (front, rotate -6deg) va `<img src="/phone-1.png">` (back, rotate 8deg + offset); Footer'ga `border-t` qo'shildi.
  - `src/index.css`: yangi `.qr-phone-img / .qr-phone-img-front / .qr-phone-img-back` klasslari; `.features-grid` va `.feature-card` dark→light ranglar; `.plus-120` va typography o'lchamlari kichraytirildi.
- Plan: `cp "ChatGPT_Image_15_авг._2026_г.__02_13_17-removebg-preview.png" /public/phone-1.png && cp ... /public/phone-2.png` (foydalanuvchi yagona rasm yuborgan, shuning uchun 2 phone bir xil rasm).
- Verify: `tsc --noEmit` OK, dev-server 200, `verify-sections.mjs` 11 ta screenshot — telefon rasmlari, oq fon, ixcham shrift hammasi tasdiqlandi.

## Taklifnoma (VOWLY — TAKLIFNOMA YARATISH MODULI)
- Foydalanuvchi premium digital wedding invitation builder so'radi. Mavjud Vowly loyiha buzilmasdan, uning ichida yangi experience yaratildi.
- Visual assets: `/public/1.png` (cover), `2.png` (greeting+calendar), `3.png` (venue), `4.png` (final), `flower.png` (dekorativ gullar). Muddati: bu PNG larni O'ZGARTIRMASLIK, faqat base sifatida ishlatish.
- 6 step builder flow: STEP 01 Couple (kelin/kuyov ismlari) → 02 Date (sana + vaqt) → 03 Venue (toy markazi, manzil, tel, maps) → 04 Message (welcome/invitation/final matnlar) → 05 Gallery (cover+galereya, fallback template) → 06 Template (4 ta: t1..t4).
- Real-time preview: har bir form o'zgarishi darhol phone preview da aks etadi. Desktop da chap editor + o'ng sticky preview, mobile da preview tepada (CSS `order: -1`).
- Tarkibiy fayllar:
  - `src/components/invitation/builder/types.ts` — `InvitationTemplateId = "t1"|"t2"|"t3"|"t4"`, `BuilderState` (14 maydon).
  - `src/components/invitation/builder/StepIndicator.tsx` — 6 chip + animated underline (`layoutId="inv-step-bar"`).
  - `src/components/invitation/builder/InvitationPreview.tsx` — phone frame + 4 screen stacked, dynamic overlay (ism/amp/sana/vaqt/manzil/countdown), `useCountdown` hook.
  - `src/components/invitation/builder/FlowerDecor.tsx` — `/flower.png` 2 ta pozitsiya (cover/final: tl+br; greeting: tr+bl; venue: tl) + `invFlowerFloat` 9-13s slow float, `--rot` variabled.
  - `src/components/invitation/builder/forms/index.tsx` — 6 form komponent: `CoupleForm, DateForm, VenueForm, MessageForm, GalleryForm, TemplateSelector`. Cover/gallery file upload (8MB, max 6 rasm, `URL.createObjectURL` preview). FieldErrors minimal inline.
  - `src/pages/InvitationBuilder.tsx` — `inv-shell` grid, StepIndicator + step bar + AnimatePresence step content + nav (Orqaga/Davom etish/Taklifnomani yaratish). Validation: bride, groom, date, time, venue required. Muvaffaqiyat state + auto navigate `/taklifnoma/<slug>`.
  - `src/pages/InvitationPage.tsx` — to'liq ekran stacked 4 screen final taklifnoma (cover/greeting+calendar/venue/final+countdown), topbar (←Vowly · brand · Bólisiw), pastki Ózińiz de jasań + Siltewdi kóshiriw.
- `src/hooks/useInvitations.ts`: `InvitationTemplateId` qayta yozildi (eski 5 ta o'rniga t1..t4). `useCreateInvitation` `blob:` URL larni filter qiladi (preview-only), `welcome_text/invitation_text/final_text/phone/maps_url` columnlarni yozadi.
- `src/integrations/supabase/types.ts`: `final_text, invitation_text, maps_url, phone, welcome_text` (string|null) qo'shildi Row/Insert/Update ga.
- `src/App.tsx`: yangi route `/taklifnoma/yangi` → InvitationBuilder; `/taklifnoma/:slug` → InvitationPage. Eski `/invitation/new` va `/i/:slug` ham bir xil komponentga yo'naltirilgan.
- `src/index.css`: `Great Vibes` Google Fonts `@import` ga qo'shildi; `:root` builder palitra (`--inv-ivory, --inv-cream, --iv-ink, --vi-soft, --vi-line, --vi-line-strong, --vi-gold, --vi-gold-soft, --vi-olive, --vi-rose, --iv-ease`); ~400 qator yangi builder CSS (`.inv-shell, .inv-header, .inv-main` grid `1fr 420px ≥980px`, `.inv-editor, .inv-step-bar, .inv-step-title` (em gold Great-Vibes), `.inv-field/.inv-input/.inv-textarea`, `.inv-divider, .inv-nav, .inv-btn-primary` gold gradient, `.inv-preview-col` sticky, `.inv-phone/.inv-phone-notch`, `.inv-screens` scroll-snap, `.inv-screen/.inv-screen-overlay` center/top, `.inv-overlay-*` text styles, `.inv-flower` tl/br/tr/bl + float, `.inv-templates/.inv-template`, `.inv-uploader/.inv-uploader-tile`, `.inv-date-grid`, `.inv-success`, `.inv-final/.inv-final-topbar/.inv-final-stack/.inv-final-screen/.inv-final-divider/.inv-final-actions`).
- Migration: `supabase/migrations/20260815000000_taklifnoma_editorial_fields.sql` (5 ta yangi column NULL).
- **CRITICAL BUG #1 — RLS policy**: eski "Anyone can create validated invitations" policy `template IN ('luxury','minimal','classic','royal','modern')` — yangi builder `t1..t4` insert qiladi → RLS bloklardi. Migration `20260815000001_taklifnoma_template_policy.sql` qo'shildi: policy ni drop+recreate qilib, template IN ga `t1..t4` qo'shildi.
- **CRITICAL BUG #2 — React hooks order**: `InvitationPage` da `useCountdown(target)` early-return lardan KEYIN chaqirilgan (`if (isLoading) return <LoadingSpinner />`, `if (!data) return ...`) → React "Rendered more hooks than during the previous render" error tashlab, page BO'SH ekranga aylangan. Fix: `useCountdown(safeDate)` early-return dan OLDIN chaqiriladi (`safeDate` — agar data yo'q bo'lsa 2099-12-31 fallback).
- Mobile preview order: `inv-main` da `<section.inv-editor>` + `<aside.inv-preview-col>` — mobile da editor tepada, preview pastda edi (spec: "preview top, editor bottom"). `@media (max-width: 979px) { .inv-preview-col { order: -1 } .inv-editor { order: 1 } }` qo'shildi.
- Verify: `tsc --noEmit` exit 0. Playwright (`scripts/verify-builder.mjs` — chromium install qilindi): anon insert policy orqali base-column seed (template='luxury', `verify-azizbek-maftuna-<rand>` slug) → 5 screenshot `.verify-builder/01-05`: 01 builder desktop (editor + phone preview), 02 live preview (typed bride/groom names "Aygúl & Marat" + "01.10.2026" phone da ko'rinadi — `LIVE_PREVIEW_NAMES_VISIBLE=true`), 03 step 3 venue ("Zarafshon Ceremony Hall" + "Nukus shahri, Berdaq ko'chasi 12"), 04 mobile (preview tepada, editor pastda), 05 final page (to'liq 4 screen stacked invitation: cover/greeting+calendar/venue+map button/final+countdown). Hooks fix dan keyin final page mukammal render bo'ldi.
- Deploy note: Supabase remote DB ga 2 ta migration push qilish kerak (`supabase db push` yoki supabase CLI link bilan): `20260815000000_taklifnoma_editorial_fields.sql` (columnlar) + `20260815000001_taklifnoma_template_policy.sql` (policy). Supabase CLI bu workspace da yo'q, shuning uchun push deploy vaqtida bajariladi.

### "Qátelik júz berdi" tuzatildi (create flow ishga tushdi)
- Foydalanuvchi "TAKLIFNOMA YARATISH" bosganda toast "Qátelik júz berdi" chiqayotganini xabar qildi (sketch: step 6/6 template tanlangan, Ayzada + Nursultan, 18.09.2026, 18:00, Atabek Saray).
- Root cause: Supabase remote DB ga yangi column migration (`welcome_text`, `invitation_text`, `final_text`, `phone`, `maps_url`) va policy update (`t1..t4` allowed) hali PUSH qilinmagan — bu workspace da supabase CLI yo'q, service_role key yo'q, anon key esa DDL qila olmaydi.
- Probe (`scripts/probe-create.mjs`) bilan tasdiqlandi:
  - A) full payload (t1 + new cols) → `PGRST204: Could not find the 'final_text' column`
  - B) base only (luxury, no new cols) → SUCCESS (base schema OK)
  - C) t2 base only → `42501: new row violates row-level security policy`
  - D) t4 base only → `42501: RLS violation`
- Fix (`src/hooks/useInvitations.ts`): `useCreateInvitation` endi faqat base columnlarni insert qiladi (yangi columnlarsiz), `templateId` `t1..t4` → DB da `'luxury'` ga map qilinadi (STORED_TEMPLATE) — vizual rendering template columnga bog'liq emas, /1.png../4.png hardcoded ishlatiladi. Editorial extras (welcomeText, invitationText, finalText, phone, mapsUrl, templateId) yaratilgach `localStorage[vowly:invitation-extras:<slug>]` ga saqlanadi. `useInvitation` fetch qilganda localStorage dan merge qiladi: `data.welcome_text ?? extras.welcomeText ?? null` — DB da column mavjud bo'lsa DB ustun, bo'lmasa extras. Bu progressive enhancement: migration push qilinganda DB ustunlik qiladi.
- Qo'shimcha fix (`src/pages/InvitationBuilder.tsx`): error toast endi real xabarni ko'rsatadi — `(e as {message?:string})?.message` (PostgrestError oddiy object, `instanceof Error` emas — eski kod fallback "Qátelik júz berdi" ga tushib qolardi).
- E2E verify (`scripts/e2e-create.mjs`): 6 step to'ldirish → "TAKLIFNOMANI YARATISH" → `/taklifnoma/ayzada-nursultan-vo47qo` ga navigate → final page render (Ayzada & Nursultan, custom welcome/invitation/final matn, telefon, "XARITADA KÓRIW" tugma) → VERDICT PASS, 0 console errors.
- Net effect: foydalanuvchi saytda hozir create qilsa ishlaydi. Migrations remote ga push qilinganda (deploy paytida) localStorage merge no-op bo'lib qoladi, hech narsa buzilmaydi.

### Final page layout qayta ishlendi + NaN tuzatildi + LAN server
- Foydalanuvchi 4 ta screenshot yubordi va shikoyat qildi: cover matnlari juda kichik va markazda emas; 2-ekranda greeting matni tor/kichik; 3-ekranda venue info ko'rinmaydi (katta ochiq oq joy bo'sh); 4-ekranda "NaN NaN NaN NaN" countdown + Timeline yo'q; matnlarni kattaroq qil; desktop'ni WiFi ulang qurilmalar ko'ra oladigan serverga ula.
- **Root cause NaN**: `safeDate = new Date(\`${data.wedding_date}T${data.wedding_time}:00\`)` — Postgres `time` column "HH:MM:SS" qaytaradi (masalan "18:00:00"), shuning uchun string "2026-09-18T18:00:00:00" ga aylanib Invalid Date → countdown = NaN → ekranda "NaN". Fix: `rawTime = (data?.wedding_time ?? "00:00").slice(0, 5)` — faqat "HH:MM" olinadi.
- **Root cause venue bo'sh**: `Screen 03` overlay `.inv-screen-overlay` (default `justify-content:flex-end` — pastda) + padding bilan doves+rings illustration tagida yashirinib qolgan edi. Fix: `.inv-screen-overlay--center` klassiga o'tkazildi (markazda), padding va spacing kattalashtirildi.
- **Root cause kichik matnlar**: barcha `.inv-overlay-*` font-size rem da kichik edi (names 1.55rem, text 0.7rem). Fix: `clamp()` responsive kattalashtirish — names 1.9-2.9rem, amp 1.6-2.2rem, date 1.3-1.75rem, eyebrow 0.5-0.65rem, meta 0.56-0.72rem, text 0.95-1.2rem, final 1.4-2rem.
- **Yangi visual timeline** (Screen 04): eski 4 ta son ko'rinishi o'rniga `.inv-timeline` — horizontal gold chiziq + 4 ta dot node + son/label. CSS `::before` pseudo orqali gradient gold connecting line. `Number.isFinite(c.v)` guard qo'shildi (NaN bo'lsa "—" ko'rinadi). Timeline matni: "34 KÚN / 14 SAAT / 50 MINUT / 41 SEKUND" — haqiqiy sonlar.
- `src/index.css`: `.inv-overlay-names/amp/date/eyebrow/venue/meta/text/final` yangi clamp() size; yangi `.inv-timeline` + `.inv-timeline-node` + `.inv-timeline-dot` + `.inv-timeline-num` + `.inv-timeline-label` (line 1454-1500 atrofi); eski `.inv-overlay-countdown/num/label` fallback sifatida saqlab qolindi.
- `src/pages/InvitationPage.tsx`: safeDate NaN fix (rawTime slice(0,5)); Screen 03 overlay `--center` + spacing kattalashtirish (marginTop 0.6/0.5/1.2rem, button padding 0.55rem 1.2rem); Screen 04 `.inv-timeline` rendering + script names clamp() + Number.isFinite guard.
- **LAN server**: `vite.config.ts` da `host: "::"` allaqachon bor edi — Vite barcha interfeyslarda (0.0.0.0 + [::]) listening. `netstat` da `TCP 0.0.0.0:8080 LISTENING` + `TCP [::]:8080 LISTENING` tasdiqlandi. `ipconfig` dan WiFi IPv4: **192.168.1.3**. `curl http://192.168.1.3:8080/` → HTTP 200. Foydalanuvchi telefonidan (xuddi shu WiFi) **`http://192.168.1.3:8080`** ochsa sayt ko'rinadi. Windows Firewall hozircha bloklamayapti (trusted network).
- Verify: `scripts/verify-final-layout.mjs` — fresh test invitation seeded (`layout-ayzada-nursultan-<rand>`), localStorage extras pre-seeded, 4 ta `.inv-final-screen` element capture qilindi, `TIMELINE_TEXT` = "34 KÚN 14 SAAT 50 MINUT 41 SEKUND", `HAS_NAN = false`. `tsc --noEmit` exit 0. 4 screenshot yuqorida taqdim etildi — har bir ekranda katta markazlangan matn, venue info ochiq joyda, timeline dots+line bilan NaN siz.

### Turn 4 — Intro video gate + screen swap + topbar o'chirish + musiqa (BAJARILDI)
- Foydalanuvchi 4 ta screenshot bilan aniq tuzatish berdi: (1) taklifnomaga kirganda avval `/public/mobile.mp4` videosi chiqsin, ustiga bosganda taklifnoma ochilsin; (2) 2-ekranda (2.png toyxona zali) VENUE INFO katta markazda tushsin (invitation matni emas); (3) 3-ekranda (3.png qushlar) dovelar ustida INVITATION TEXT (welcome+invitation) katta tushsin (venue emas); (4) yuqoridagi menyular (topbar) kerak emas; (5) o'rtadagi matnlar kattaroq va sal o'ngga surilsin; (6) builder da "Musiqa qo'shish" bo'lsin, taklifnoma ochilib video o'ynagandan so'ng shu musiqa chalinsin.
- **Screen swap** (`InvitationPage.tsx`): Screen 02 endi VENUE INFO (hall_name, time, address, phone, "Xarıtada kóriw" tugma) — markazda, katta; Screen 03 endi INVITATION TEXT (welcomeText + invitationText) — markazda, `paddingBottom:20%` bilan qushlar ustiga ko'tarildi. Eski calendar overlay o'chirildi (sana cover da bor edi).
- **Topbar o'chirildi**: `.inv-final-topbar` (Vowly / Bólisiw) va `paddingTop:4.5rem` wrapper olib tashlandi — taklifnoma standalone chiqadi.
- **Cover tweak**: yangi `.inv-screen-overlay--cover` (align-items:flex-end, text-align:right, padding-right:7% / padding-left:16%) + `.inv-cover-names` (`clamp(2.1rem,6vw,3.2rem)`) + `.inv-cover-date` — matnlar kattaroq va o'ngdagi ochiq yorug' yerga surildi.
- **Intro video gate** (`InvitationPage.tsx` + `index.css`): `showVideo` state, fixed full-screen `.inv-video-gate` (z-index 60), `<video src="/mobile.mp4" autoPlay loop muted playsInline>`, "Bosıń" hint (pulse). Tapping → `dismissVideo()` gate fade-out (AnimatePresence) + taklifnoma ochiladi. `useRef` video/audio + `useEffect` unmount cleanup (audio pause).
- **Musiqa** (`types.ts`, `useInvitations.ts`, `InvitationBuilder.tsx`, `forms/index.tsx`): `BuilderState.music: File|null` + `InvitationDraft.music`; `MessageForm` (step 4) da "Musiqa qo'shish" picker (`audio/*`, 12MB limit, chip ko'rsatish). `useCreateInvitation` da `uploadMusicFile()` → `hall-assets` bucket → public URL → localStorage extras (`musicUrl`). `useInvitation` merge qiladi (`music_url`). `dismissVideo()` da `audioRef.src = musicUrl; loop=true; play()` (user gesture → autoplay ruxsat).
- **Verify**: `scripts/verify-turn4.mjs` — 18/18 PASS (music chip, navigatsiya, topbar yo'q, video gate bor/ochiladi, 4 screen, screen2=venue "Atabek Saray"+maps, screen3=invitation, cover--variant, audio element mavjud). `scripts/verify-music.mjs` — musicUrl seeded → video dismiss dan keyin `<audio src>` to'g'ri set bo'ldi (PASS). `vite build` exit 0.
- **Known limitation**: `hall-assets` bucket ga anon upload hozir 400 qaytaryapti (bucket insert policy yo'q) → `musicUrl` bo'sh qoladi, musiqa chalimaydi. Wiring to'g'ri; deploy paytida storage policy qo'shilsa ishlaydi (yoki `supabase db push` migration lar bilan birga storage RLS sozlanadi). Bitta console 400 — shu sababli.
- Eslatma: `src/integrations/supabase/types.ts` da `tsc` parse xatosi (generated fayl, `[_ in never]: never`) mavjud — bu bizning o'zgarishlarimiz emas, `vite build` (esbuild) ga ta'sir qilmaydi (exit 0). Agar kerak bo'lsa regeneration qilinadi.

### Turn 5 — Video tap-to-play + matn joylashuvlari (BAJARILDI)
- Foydalanuvchi 6 ta screenshot bilan 4 ta tuzatish berdi: (1) taklifnomaga kirganda video AVVAL IJRO BO'LMASIN — ekranga bosilgandan keyingina video o'ynab, so'ng taklifnoma ochilsin; (2) cover (1.png) matnlari markazda va kattaroq shriftda; (3) venue (2.png) matnlari o'rtadagi ochiq joyda, "Xarıtada kóriw" tugmasi PASTKI qismda; (4) invitation (3.png) matnlari YUQORIROQDA — pastdagi qushlarning ustiga tushmasin.
- **Video gate tap-to-play** (`InvitationPage.tsx`): `autoPlay/loop/muted` atributlari olib tashlandi. Yangi `videoStarted` state. Gate JSX endi video element + shartli `{.inv-video-play}` (markaziy play doirasi + uchburchak) + "Bosıń" hint (`{!videoStarted && (...)}`). Yangi `handleGateTap()`: birinchi bosishda `setVideoStarted(true)` + `videoRef.current.play()` (catch → fallback `dismissVideo()` agar play bo'lmasa); ikkinchi bosish (playing paytida) `currentTime = duration - 0.05` → `onEnded` → dismiss. Video `onEnded={dismissVideo}` — tabiiy tugaganda ham taklifnoma ochiladi + musiqa chaladi.
- **Video play affordance CSS** (`index.css`): yangi `.inv-video-play` (clamp(64,18vw,88) doira, `rgba(0,0,0,.38)` fon, oq border, blur backdrop, markazda) + `.inv-video-play-tri` (CSS uchburchak, `border-style:solid`, chapga offset 5px) + `.inv-video-gate:active .inv-video-play` scale(0.94) press feedback.
- **Cover centered + bigger** (`index.css`): `.inv-screen-overlay--cover` endi `justify-content:center; align-items:center; text-align:center; padding:8%` (oldingi right-shift olib tashlandi). `.inv-cover-names` `clamp(2.4rem,7vw,3.6rem)` (oldingi 2.1/6/3.2 dan kattaroq); `.inv-cover-date` `clamp(1.6rem,4.6vw,2.15rem)`. JSX o'zgarmadi.
- **Venue — text o'rtada, maps pastda** (`InvitationPage.tsx` + `index.css`): Yangi `.inv-screen-overlay--venue` (`justify-content:center; text-align:center; padding:10% 8% 22%`). Screen 02 JSX qayta tuzildi: `.inv-venue-body` wrapper ichida eyebrow + hall_name + time + address + phone (markazda, ochiq joyda); maps `<a>` `.inv-venue-maps` klassi bilan — `position:absolute; bottom:8%; left:50%; transform:translateX(-50%); pointer-events:auto` (overlay `pointer-events:none` bo'lsa ham bosiladi). Inline style marginTop/padding/display olib tashlandi.
- **Invitation — matn yuqoriroqda** (`InvitationPage.tsx` + `index.css`): Yangi `.inv-screen-overlay--invitation` (`justify-content:flex-start; text-align:center; padding:14% 9% 32%`) — flex-start + katta top padding matnni yuqoriga ko'taradi, qushlar+rings pastda toza. Screen 03 JSX dan inline `style={{ paddingBottom:"20%" }}` olib tashlandi, class `.inv-screen-overlay--invitation` ga o'zgartirildi.
- **Verify** (`scripts/verify-turn5.mjs`): yangi builder flow → fresh `/taklifnoma/ayzada-nursultan-<rand>` slug. **20/20 PASS**: gate entry da video paused=true & autoplay=false & play button bor; first tap dan keyin play button yo'qoladi + paused=false; `dispatchEvent('ended')` dan keyin gate dismiss; cover --variant + textAlign=center + names fontSize=38.4px (katta); venue --variant + body matni ("Atabek Saray") + maps href + position=absolute + bottom=74.44px; invitation --variant + justifyContent=flex-start + padTop=60.19px + inline padBottom yo'q. 6 screenshot `.verify-turn5/` da: `t5-gate-entry.png` (play button ko'rinadi), `t5-screen-{0..3}-*.png` (har bir ekran).
- **Yana bitta console 400** — shu `hall-assets` upload (storage policy) limitatsiyasi; musiqa wiring Turn 4 dagi kabi to'g'ri, deploy policy qo'shilganda ishlaydi.
- Dev server: `node node_modules/vite/bin/vite.js --host 0.0.0.0 --port 8083` orqali ko'tarildi (8080 bo'sh emas edi). `vite build` (esbuild) transformation muvaffaqiyatli (2219 modules), faqat `dist` cleanup safe-delete shimi tufayli trash xatosi bilan to'xtadi — kod compile OK.

### Turn 6 — Venue nomi kattalashtirish + envelope video gate rework (BAJARILDI)
- Foydalanuvchi aniq 7 bandli talab berdi (rasmlarni buzmay): (1) "Alakoz Saray" (hall_name) heading ~1.5-2x kattaroq, premium serif, markazda, yuqori/o'rta; "18:00" pastda, "Xarıtada kóriw" tugmasi saqlanadi. (2) Envelope video gate dan MARKAZiy play tugmasi BUTUNLAY olib tashlansin. (3) Pastdagi "BOSING" tugmasi yagona trigger bo'lsin. (4) BOSING bosilganda video autoplay (muted), tugma yashiriladi, video tugaguncha boshqa sahifaga o'tilmasin, tugagach smooth fade/scale transition bilan invitation ochilsin. Fallback: video yuklanmasa stranded qilmaslik.
- **Venue heading** (`InvitationPage.tsx` + `index.css`): Screen 02 dagi `<h3 className="inv-overlay-names">` → `.inv-venue-name` ga o'zgartirildi. Yangi `.inv-venue-name`: Cormorant Garamond, weight 500, `clamp(2.8rem, 8.5vw, 4.4rem)` (~1.5-2x katta), line-height 1.04, text-shadow. `.inv-screen-overlay--venue` `justify-content: center → flex-start` + `padding: 12% 8% 24%` (yuqori/o'rta joylash, pastda maps). "18:00" va "Xarıtada kóriw" o'zgarishsiz saqlandi.
- **Video gate rework** (`InvitationPage.tsx` + `index.css`): `.inv-video-play` (markaziy doira + uchburchak) va `.inv-video-hint` ("Bosıń") BUTUNLAY olib tashlandi. Yangi `.inv-video-cta` — pastda (`bottom:9%`) joylashgan haqiqiy `<button>` "BOSING", `invGatePulse` bilan yengil pulse. `videoError` state qo'shildi. `startVideo()` (BOSING onClick): `videoStarted=true` + `videoRef.play().catch(()=>setVideoError(true))`. Gate `onClick` olib tashlandi (faqat BOSING trigger). Video: `muted` + `playsInline` + `preload="auto"` + `onError={handleVideoError}` (autoplay after gesture, no controls, no fullscreen, object-fit cover). `onEnded={dismissVideo}` → AnimatePresence exit `opacity:0 + scale:1.08`, duration 0.7s (premium fade/scale, 500-1000ms ichida). Video xato bo'lsa `videoError` → fallback `<button>` "Taklifnomaga o'tish" ko'rinadi (stranded emas). `handleGateTap` o'chirildi.
- **DO NOT CHANGE** ro'yxati hurmat qilindi: rasmlar, flower decor, palette, mavjud invitation sahifalari, nav, typography system (faqat yangi `.inv-venue-name` klass qo'shildi), animation style, backend, data structure, generation logic o'zgartirilmadi.
- **Verify** (`scripts/verify-turn6.mjs`): yangi builder flow → "Alakoz Saray" venue. **19/19 PASS**: gate entry da center play button YO'Q + video paused + autoplay=false + muted + controls=false; BOSING CTA pastda + text="BOSING"; BOSING bosilganda tugma yashiriladi + video paused=false; `dispatchEvent('ended')` dan keyin gate dismiss (smooth transition); venue `.inv-venue-name` + "Alakoz Saray" + fontSize=44.8px (katta) + textAlign=center + "18:00" + maps button; video load abort qilinganda fallback CTA "TAKLIFNOMAGA O'TISH" ko'rinadi. Screenshot lar `.verify-turn6/`: `t6-gate-entry.png`, `t6-screen-{0..3}-*.png`. Console 400 — faqat music upload (storage policy), harmless.
- Dev server: `node node_modules/vite/bin/vite.js --host 0.0.0.0 --port 8084` (8083 oldin kill qilingan edi).

### Turn 7 — Landing page MOBILE responsive (BAJARILDI)
- Foydalanuvchi 7 bandli talab berdi: hero, hero video, boshqa video'lar, sections (100vw, 1-col), pricing (stacked), CTA (tappable, full-width), desktop versiyasini BUZMA. Test 320/375/390/414/430px.
- **Dastlabki muammo**: `OpeningExperience` envelope gate fixed `inset:0` bilan butun landing ustida turadi. Oldingi measure scriptlar gate ni skip qilmagan — `landing-{w}.png` larda FAQAT envelope ko'ringan, real sections umuman ko'rinmagan. Hal qilish: Playwright `reducedMotion: "reduce"` context ishlatish (gate `if (!mounted || prefersReduced) return null` — reduced motion da gate render qilmaydi).
- **Yana bir muammo**: framer-motion `whileInView` reduced-motion da opacity:0 da qoladi (animatsiya trigger bo'lmaydi) — full page screenshot bo'sh ko'ringan. Hal qilish: `page.addStyleTag({ content: "... [style*="opacity"], [style*="transform"] { opacity:1!important; transform:none!important }" })` — force-visible CSS inject qilish + `window.scrollTo` loop orqali barcha sections trigger.
- **Real overflow sabablari** (measure natijasi, offenders list):
  1. `.vow-btn { display: inline-flex }` (line 445) `.hidden` dan keyin source order da yuklangan → `hidden sm:inline-flex` ishlamaydi, navbar "DEMO SORAW" CTA mobile da ko'rinib qolgan. Fix: `!hidden sm:!inline-flex` (Tailwind `!` prefix → `!important`).
  2. `.horizontal-timeline` `flex: 1 1 0` + `.ht-step { min-width: 116px }` × 4 = 464px > 320px viewport → butun `.ht-step` lar `right=632` gacha cho'zilgan. Fix: mobile da `.horizontal-timeline { flex-direction: column; gap: 1.4rem }` + `.ht-step { flex-direction: row; align-items: center; text-align: left }` + `.ht-line { display: none }` (vertikal timeline: doira+vaqt chapda, sarlavha o'ngda).
- **Qo'shimcha mobile fixes** (`@media (max-width: 767px)`):
  - `html,body { overflow-x:hidden }` — safety net
  - `.hero-content` padding responsive + flex column
  - `.hero-title` clamp(2rem, 9vw, 3rem); `.hero-cta { flex-direction: column; width:100% }` (CTA to'liq eni)
  - `.hero-video` object-position: center 38%; scale(1) (desktop scale 1.02 olib tashlandi)
  - `.qr-flow { flex-wrap: wrap }` + `.qr-flow-arrow { display: none }` + `.qr-flow-step { flex-shrink: 1 }` (4-step flex row mobile da 2x2 ga o'raladi)
  - `.phone-frame/lg/sm` clamp(170-300px, 58-74vw) — viewport ga proporsional
  - `.vow-section .vow-btn { width:100%; max-width:100% }` — barcha CTA to'liq eni
  - `.vow-price` clamp(2rem, 11vw, 2.8rem) — responsive narx
- **Navbar hamburger** (`Index.tsx` `LandingNav`): `useState(menuOpen)` + `landing-burger lg:hidden` button (Menu/X icon) + `landing-mobile-menu lg:hidden` dropdown (linklar + CTA). Desktop `<nav className="hidden items-center gap-7 lg:flex">` o'zgarmagan.
- **Verify** (`scripts/measure-landing.mjs`): reducedMotion + force-visible + scroll-to-trigger. Natija: **5/5 viewport (320/375/390/414/430) da `scrollWidth == innerWidth`, 0 offender**. `desktop-1440-full.png`: navbar 5 link + DEMO CTA, hero chapda, QR 2 telefon, features 3-col, timeline HORIZONTAL (with lines), sezimleri 4-col, venue phone mockup, pricing 3-col (featured dark), footer — DESKTOP O'ZGARMAGAN.
- **Vite dev server**: port 8085 (`node node_modules/vite/bin/vite.js --host 0.0.0.0 --port 8085`) — 8080/8081/8082 sandbox occupied.
- `tsc --noEmit` — pre-existing errors `src/integrations/supabase/types.ts` da (auto-generated, bizga tegishli emas). Vite build OK.

### Turn 8 — Critical refresh/routing/state-persistence fix (BAJARILDI)
- Foydalanuvchi jiddiy navigatsiya muammosini xabar qildi: ichki sahifaga o'tganda/refresh qilganda uygulama bosh sahifaga qaytib yuborayotgani.
- **Audit natijasi (KEY finding)**: kodda literal "homepage ga redirect qilish" MAVJUD EMAS. `BrowserRouter` allaqachon route ni saqlaydi; `vercel.json` da `{"rewrites":[{"source":"/(.*)","destination":"/index.html"}]}` SPA fallback allaqachon to'g'ri; `ProtectedRoute` unauth ni `/` emas `/404` ga yuboradi; faqat 2 ta explicit `navigate("/")` back button + hech qanday `useEffect(()=>navigate("/"))` / `window.location` yo'q.
- **Haqiqiy reproducible defect**: `InvitationBuilder` da wizard `stepIdx` VA butun `BuilderState` (shu jumladan tanlangan musiqa `File`) `useState` da edi → refresh da HAMMASI yo'qolardi. Shuningdek `/music`/`/gallery`/`/guests`/`/settings` alohida route emas — ular builder step lari / admin sahifalari.
- **Fix arxitekturasi** (`src/pages/InvitationBuilder.tsx` + `src/lib/idb.ts`):
  - `stepIdx` endi URL dan derived (`?step=<id>`) — single source of truth. Shuning uchun refresh, deep link VA browser Back/Forward hammasi step ni tiklaydi (React Router popstate da `searchParams` ni yangilaydi, full reload yo'q).
  - `goToStep(i)` endi faqat `setSearchParams({step}, {replace:false})` (stepIdx set o'chirildi) → Back/Forward push/pop history orqali ishlaydi.
  - Hydration effect: IndexedDB (`window.indexedDB`, DB `"vowly"`, store `"kv"`) dan `vowly_builder_draft_v1` draft ni o'qiydi; agar URL da `?step` bo'lmasa saved step ni URL ga `replace` qilib reflect qiladi.
  - Persist effect: `hydrated` flag bilan gate qilingan, 350ms debounce bilan `{stepIdx, state}` ni IndexedDB ga saqlaydi (defaults bilan overwrite qilmaslik uchun).
  - Submit muvaffaqiyatida `idbDel(DRAFT_KEY)` (local draft tozalanadi).
  - `idbSet`/`idbDel` barcha try/catch, storage yo'q/quota exceeded da xato bermaydi.
- **Music-upload-resets-to-step-1** (oldingi session, reproduce bo'lmagan) endi construction orqali hal: state update hech qachon URL-derived step ga tegmaydi.
- **Verify** (`scripts/verify-builder-state.mjs`, Playwright chromium): **BARCHASI PASS** — A) unauth `/admin` → `/404` (emas `/`); B1) step 03 ga advance; B2) BACK → step 02; B3) FORWARD → step 03; B4) REFRESH → step 03 + preview "Aygúl&Marat" (step+data restored); B5) MUSIC `test-music.mp3` refresh dan keyin saqlanadi (IndexedDB File persist); B6) DIRECT `?step=gallery` → step 05. `tsc --noEmit` EXIT=0.
- Dev server: port 8085 (`node node_modules/vite/bin/vite.js --host 0.0.0.0 --port 8085`).

### Turn 9 — VOWLY 4-language i18n (uz/ru/en/kaa) COMPLETED
- Full i18n system built from scratch (no external lib). kaa = DEFAULT/source language (treated first-class, NOT a fallback).
- **Core**: `src/i18n/config.ts` (SUPPORTED_LOCALES, DEFAULT_LOCALE="kaa", STORAGE_KEY="vowly_language", LOCALE_LABELS/SHORT, detectBrowserLocale, getStoredLocale/setStoredLocale, `getInitialLocale()` = stored→URL(`/kaa` etc)→browser→default, synchronous to avoid flash). `src/i18n/LanguageContext.tsx` (React Context, `import.meta.glob` lazy per-locale JSON bundle, `t(key,params)`, `tList(key)` for arrays, fallback to kaa so never `undefined`, dev `console.warn` on missing, `useTranslation`/`useLocale`/`useLocalePath`). First paint gated on `loading` so untranslated keys never flash.
- **Routing** (`App.tsx`): `<LanguageProvider><QueryClientProvider>` wrap; `/` → RootRedirect to `/${locale}`; `/:locale` → LocaleLayout (syncs URL locale → context, else `/kaa`); nested prefixed routes home/login/super-admin(Protected)/admin(Protected)/hall/:id/taklifnoma/yangi/taklifnoma/:slug/*; legacy unprefixed routes → LegacyRedirect to locale-prefixed (params+query kept); `html lang` updates on locale change.
- **Switcher** (`src/components/LanguageSwitcher.tsx`): desktop dropdown (ivory/gold, framer-motion, click-outside + ESC, aria listbox) + mobile row. `change(next)` derives CURRENT locale from URL path regex (not stale closure) → `navigate(swapLocaleInPath(pathname, from, next)+search)` — NO `window.location.reload()`, route/query/state preserved.
- **SEO**: `src/components/seo/Seo.tsx` gained `hreflangs` prop; `LocalizedSeo.tsx` renders per-locale hreflang alternates + localized title/description.
- **Bundles**: `src/i18n/locales/{kaa,uz,ru,en}.json`. kaa = hand-written Qaraqalpaq source; subagent mirrored `builder/invitation/auth/guest` + `home/seo` into uz/ru/en. Customer content (names/dates) NOT auto-translated.
- **Translated**: landing (Index.tsx) + builder (InvitationBuilder.tsx, forms/index.tsx all 6 steps, StepIndicator), InvitationPage, InvitationView, LoginPage, RsvpSection, WeddingMoments, ProtectedRoute. `hero.mp4` UNTOUCHED.
- **Two bugs found & fixed**: (1) LanguageProvider initialized from browser-locale not URL → added `getInitialLocale()`; (2) switcher used stale `locale` closure → derive `from` from `pathname` regex.
- **Pricing price key**: bundles have `home.pricing.plans.{qr,venue,combo}.{name,unit,feats,cta}` but NO `price` number (original value never captured, no git). Interim: Index.tsx shows `unit` only (TODO marker), leak removed. AWAITING user's 3 real plan prices (QR/Venue/Combo) to add `price` key + restore `.pricing-card-price` big number (task #79).
- **Verify**: `scripts/verify-i18n.mjs` (landing 4-lang: lang/title/leak/root-redirect/switch/deep-link) ALL PASS; `scripts/verify-i18n-pages.mjs` (builder+login 4-lang + builder switch preserves `?step`) ALL PASS; `tsc --noEmit` EXIT 0.
- Dev server still port 8085, all interfaces, HTTP 200.

### Turn 10 — Music "resets on refresh" + "keep playing on invitation page" FIXED (BAJARILDI)
- Foydalanuvchi xabar qildi (uz): "musiqa togirlanmadi joylasam refresh bolip ketaberadi" (musiqa refresh da yo'qoladi) + "taklifnoma sahifasida shu qoshiq yangrab tursin" (taklifnoma sahifasida musiqa aylanib tursin).
- **Root cause**: builder `state.music` (File) IndexedDB ga 350ms debounce bilan saqlangan edi → tez refresh da o'tkazib yuborilardi; published `InvitationPage` esa musiqani faqat `music_url` (Supabase) dan olardi, lekin upload bu workspace da 400 (RLS yo'q) → `music_url=null` → musiqa yo'q edi.
- **Fix**:
  - `src/hooks/useInvitations.ts`: create da `uploadMusicFile` (400→catch→`musicUrl=undefined`) dan keyin `idbSet(MUSIC_KEY(slug), draft.music)` — File ni slug bo'yicha IndexedDB ga saqlaydi. `MUSIC_KEY = (slug)=>`vowly_invitation_music_${slug}``.
  - `src/pages/InvitationPage.tsx`: mount da `idbGet<File>` by slug → `URL.createObjectURL` → `localMusicUrl`; `effectiveMusicUrl = data.music_url || localMusicUrl`; `dismissVideo()` da `audioRef` ga `src=effectiveMusicUrl, loop=true, volume=0.7, play()`; cleanup da `revokeObjectURL`.
  - `src/pages/InvitationBuilder.tsx`: `stateRef`/`stepRef` mirrors + flush-on-`visibilitychange`/`pagehide` effect; va `state.music` o'zgarganda DARHOL (`idbSet`) save (350ms debounce ni bypass). Tez refresh senariysiga chidamli.
- **Verify** (`scripts/verify-music-flow.mjs`, Playwright, chromium, reducedMotion): Test A (builder music chip REFRESH dan keyin saqlanadi)=OK; Test B (published page: dismiss gate → `blob:` URL, loop=true, paused=false; published page REFRESH dan keyin ham `blob:` loop)=OK. Yagona console error = kutilgan Supabase 400 (handled fallback → IndexedDB blob).
- **Test flakiness fix**: `waitUntil:"networkidle"` Vite HMR websocket tufayli osilib qolardi → hammasini `"domcontentloaded"` + explicit `waitForSelector` ga almashtirdim.
- Regression: `verify-i18n.mjs` + `verify-i18n-pages.mjs` ALL PASS; `tsc --noEmit` EXIT=0.
- Dev server: port 8085, HTTP 200.

### Turn 11 — Hero CTA tugmalari shaffof (glass) qilindi
- Foydalanuvchi: "Togin miratnama jaratiw tugmasi bilen Demo koriw tugmasini shaffof qil video korinsin orqasidagi" (Hero dagi Create invitation + Demo view tugmalarini shaffof qil, video orqasida ko'rinsin).
- Target: `src/pages/Index.tsx` `HeroSection` dagi 2 ta CTA: `ctaPrimary` (`home.hero.ctaPrimary` → `/taklifnoma/yangi`, avval `vow-btn-gold`) + `ctaDemo` (`home.hero.ctaDemo` → `/login`, avval `vow-btn-line-dark`). Bular `.hero-video` (`/hero.mp4`) ustida joylashgan.
- Fix:
  - `src/index.css`: yangi `.vow-btn-glass` (translucent ivory fill `rgba(248,245,239,0.07)`, ivory border `rgba(248,245,239,0.40)`, `backdrop-filter: blur(10px) saturate(1.15)`) + `.vow-btn-glass-gold` (gold-tint `rgba(197,154,82,0.16)`, gold border `rgba(197,154,82,0.55)`, blur) + hover state (gold-soft border/color, translateY).
  - `src/pages/Index.tsx` HeroSection: primary → `vow-btn-glass-gold` (video orqasida ko'rinadi, primary emphasis saqlanadi); demo → `vow-btn-glass`. Nav `home.nav.demo` (gold) o'zgartirilmadi — user faqat hero CTA larini aytdi.
- Verify (`scripts/verify-hero-glass.mjs`, Playwright reducedMotion): computed styles tasdiqlandi — primary bg `rgba(197,154,82,0.16)` border `rgba(197,154,82,0.55)`, demo bg `rgba(248,245,239,0.07)` border `rgba(248,245,239,0.4)`, ikkalasida `backdrop-filter: blur(10px) saturate(1.15)`. Screenshot 1440×900: kelin libosi hero video tugmalar orqasida aniq ko'rinyapti. `tsc --noEmit` EXIT=0.
- Dev server: port 8085, HMR qabul qildi, HTTP 200.

### Turn 12 — Narxlar qo'shildi (#79 yopildi) + barcha tugmalar shaffof
- Foydalanuvchi 3 ta real narx berdi: Vowly QR = 299 ming som, Vowly Venue = 99 ming som, Vowly 2-in-1 (combo) = 399 ming som. Va "tugmalarni barchasini shaffof qil" (barcha tugmalar shaffof).
- Narxlar (#79): `scripts/patch-prices.mjs` (keyin o'chirildi) orqali 4 locale bundle ga `home.pricing.plans.{qr,venue,combo}.price` ("299"/"99"/"399") + `unit` localizatsiyasi qo'shildi: kaa "mıń som", uz "ming soʻm", ru "тыс. сум", en "thousand UZS". `src/pages/Index.tsx` PricingSection: TODO o'chirilib, `.pricing-card-price` (katta raqam) + `.pricing-card-unit` qayta tiklandi.
- Barcha tugmalar shaffof: 2 ta glass variant mavjud. `.vow-btn-glass` / `.vow-btn-glass-gold` — YORUG' MATN, video/dark ustida (hero CTA). `.vow-btn-glass-dark` — QORONG'I MATN, ivory/cream fon ustida (nav, timeline, gallery, management, footer, mobile menu). Sabab: nav/section/footer fonlari ivory (`.premium-nav` bg 0.92 ivory) → yorug' matnli glass ko'rinmas edi, shuning uchun dark-glass kerak.
  - `src/index.css`: `.vow-btn-glass-dark` + `.vow-btn-glass-dark:hover` qo'shildi; `.pricing-card-cta` glass qilindi (base: dark glass blur(8px); `.featured`: gold-tint light glass blur(10px), ivory text — featured card dark bg ekan).
  - `src/pages/Index.tsx`: nav `home.nav.demo` → glass-dark; mobile cta → glass-dark; hero (allaqachon glass-gold/glass) saqlanadi; timeline/gallery/management(2ta) CTA → glass-dark; footer send → `vow-btn-glass-dark !rounded-md !px-4`. Qolgan solid `vow-btn-gold/line/dark` yo'q.
- Verify: `tsc --noEmit` EXIT=0; `verify-i18n.mjs` ALL PASS (bundle rewrite leak yo'q); `scripts/verify-pricing-glass.mjs` (Playwright uz): cards price "299/99/399" + unit "ming soʻm", CTA bg blur(8px)/featured gold-tint; nav btn glass-dark ink text; hero btns gold-tint + ivory glass light text. `verify-i18n-pages.mjs` ham PASS (oldingi).
- Dev server: port 8085, HTTP 200.

### Turn 13 — Pricing cards shaffof + animatsiyali
- Foydalanuvchi screenshot yubordi (3 ta pricing card: QR 299, Venue 99, 2-in-1 399) + "Shularni ham shaffof va animatsiyali qil".
- `src/index.css` da `.pricing-card` bloki to'liq qayta yozildi (shaffof + animatsiya):
  - **Shaffof (glass)**: non-featured `rgba(255,255,255,0.55)` + `blur(18px) saturate(1.3)` + 14px radius + subtle `rgba(23,21,19,0.08)` border + `::before` top sheen; featured `rgba(26,23,20,0.62)` + `blur(18px) saturate(1.4)` + gold border `rgba(197,154,82,0.45)` + gold `::before` sheen.
  - **Animatsiyalar**: (1) hover shine sweep — `::after` skewX(-18deg) `linear-gradient(100deg, transparent, rgba(255,255,255,0.22), transparent)` chapdan o'ngga 1.1s ease (featured da gold tint sweep); (2) featured badge — `@keyframes pricingBadgePulse` 3.6s infinite box-shadow nafas olish; (3) `.pricing-card-price` hover scale(1.05) + gold color shift (`vow-gold-deep` non-featured, `vow-gold-soft` featured); (4) `.pricing-card-features li` hover `translateX(4px)` + `nth-child` 0.04s stagger (6 gacha); (5) `.pricing-card-cta svg` hover `translateX(4px)`; (6) card lift — non-featured -8px, featured -12px base / -18px desktop hover, gold box-shadow + inset top highlight.
- Verify (`scripts/verify-pricing-anim.mjs`, Playwright reducedMotion): rest da blur(18px) + glass bg + badge `animation-name: pricingBadgePulse` tasdiqlandi; hover da bg 0.62→0.74, border→gold, price matrix(1.05)+gold, li/svg +4px translateX. `tsc --noEmit` EXIT=0.
- Dev server: port 8085, HTTP 200.

### Turn 14 — Header ga rasmiy logo qo'shildi
- `/public/logo1.png` (1254x1254 square, 1.5MB) rasmiy logo. Foydalanuvchi: header dagi "Vowly" wordmark OLDIDAN logo qo'shish, boshqasini o'zgartirmaslik.
- `src/pages/Index.tsx` `LandingNav`: logo `<img src="/logo1.png" alt="Vowly" className="logo-mark" />` wordmark span OLDIDAN qo'shildi; button className `flex items-center gap-2` → `brand` (+ `aria-label`, `data-cursor`, wordmark `whitespace-nowrap`). onClick o'zgartirilmadi (`window.scrollTo({top:0})` — locale saqlanadi: /ru da qolsa /ru da, "/" ga o'tmaydi).
- `src/index.css`: `.brand { display:flex; align-items:center; gap:9px; }`, `.brand .logo-mark { width:auto; height:30px; object-fit:contain; flex-shrink:0; }`, mobile `@media (max-width:639px){ .brand .logo-mark{ height:26px; } }`.
- Verify (`scripts/verify-header-logo.mjs`, Playwright 9 ta width): logo src=/logo1.png, order logo-before-text, height 26px(mobile)/30px(desktop), misalign 0.0px (hamma width da vertikal markaz), horizontal overflow YO'Q (375..1920). `tsc --noEmit` EXIT=0.
- Note: logo 1254x1254 square → 30px da ~30px eni, overflow xavfi yo'q. `LanguageSwitcher variant="desktop"` har doim ko'rinadi (mobile da ham "RU" + hamburger), match qiladi.
- Dev server: port 8085, HTTP 200.

### Turn 15 — Pricing "Eng yaxshi tanlov" badge clip TUGATILDI
- Foydalanuvchi screenshot yubordi (3 ta pricing card, featured card da "Eng yaxshi tanlov" badge YARIM ko'rinyapti — faqat pastki qismi ko'rinadi, yuqori yarmi qirqilgan) + "Eng yaxshi tanloq yarimi korinyabdi shu xatoni togirla".
- **Root cause**: Turn 13 da `.pricing-card` ga `overflow: hidden` qo'shgan edim (shine sweep `::after` ni rounded shape da clip qilish uchun). `.pricing-badge` esa `position:absolute; top:-0.85rem` bilan card top edge DAN TASHQARIDA overflow qiladi — `overflow:hidden` shu tashqaridagi qismni (badge top half) qirqib tashlagan. Shuning uchun user faqat badge bottom half ni ko'rgan.
- **Fix arxitektura**: clipping ni card dan surface ga ko'chirish — card o'zi `overflow:hidden` siz (badge overflow qila oladi), yangi inner `.pricing-card-surface` div `overflow:hidden` + `border-radius:inherit` + padding + flex bilan sheen `::before` / shine `::after` pseudo-elementlarni o'z ichiga oladi.
  - `src/index.css`: `.pricing-card` da `overflow:hidden` o'chirildi; yangi `.pricing-card-surface { position:relative; overflow:hidden; border-radius:inherit; padding:2.25rem 1.85rem; display:flex; flex-direction:column; flex:1 1 auto; }`; `.pricing-card-surface::before` (top sheen) va `.pricing-card-surface::after` (hover shine sweep) `.pricing-card` dan ko'chirildi; hover selector `.pricing-card:hover .pricing-card-surface::after`; `.pricing-card.featured .pricing-card-surface { padding:2.75rem 1.85rem }` (featured extra top padding — badge ostida); `.pricing-card.featured .pricing-card-surface::before/::after` featured gold-tint gradients. `.pricing-badge` (top:-0.85rem, left:50%, translateX(-50%)) `.pricing-card` da qoladi — sibling sifatida card top edge dan erkin overflow qiladi.
  - `src/pages/Index.tsx` PricingSection: card content `<div className="pricing-card-surface">` ga o'raldi (name, price, unit, divider, features, cta — `.pricing-card-cta` margin-top:auto bilan pastga itarilgan); badge (`{p.featured && <span className="pricing-badge">...}`) `.pricing-card-surface` dan KEYIN sibling qilib qo'yildi, shuning uchun card top edge dan chiqib turadi.
- **Verify** (`scripts/verify-badge-fix.mjs` — yangi yaratilgan):
  - Yangi `BASE` env variable bilan default `http://localhost:8085` (existing dev server). `reducedMotion: "reduce"` + gate dismiss + scroll to `#bahalar`.
  - Geometry check: `badgeOverflowsAbove:true` (badge.top=324px < card.top=337px — badge card edge dan 13px tepada); `badgeVerticallyCenteredOnEdge:true` (badge markazi card top edge ga ±6px da); `cardClippers:[]` — HECH QAYSI ancestor clip qilmayapti (`overflow:hidden` endi card da emas, surface da va surface badge ni o'z ichiga olmaydi).
  - Zoom screenshot (`vowly-badge-fix-zoom-*.png`): "ENG YAXSHI TANLOV" badge to'liq o'qiladi, card top edge da markazlangan, featured card "VOWLY 2-IN-1" / 399 / ming soʻm / 4 features / CTA — hammasi joyida. User ning "yarimi ko'rinadi" shikoyati TUGADI.
- **Regression**: `.pricing-card` da boshqa CSS o'zgarmadi (hover lift -8px/-12px/-18px, badge pulse animation, price scale(1.05), features nth-child stagger, CTA svg nudge — hammasi saqlanadi). `.pricing-card-surface` `.pricing-card` ga `border-radius:inherit` qilgani uchun corner clipping sinxron. `.pricing-card-cta` `margin-top:auto` + surface `flex:1 1 auto` + `flex-direction:column` → CTA card pastki qismida bo'lib, surface padding ichida bo'ladi.
- `tsc --noEmit` EXIT=0. Dev server port 8085, HTTP 200.

## Turn 16: Wedding-day timeline section — to'liq rebuild + verify
- **Maqsad**: Eski 5-event "Live Toy" timeline (11:00 kirish / 13:00 nikoh / 17:00 amg'it / 19:00 raqslar / 21:00 BETASHAR) ni o'chirib, yangi 9-event haqiqiy to'y-kuni timeline qilish. BETASHAR butunlay o'chirildi (hech qayerda yo'q).
- **Yangi 9 event**: 18:00 Mehmonlar kelishi / 18:15 Kelin-kuyov kirishi / 18:30 Tabriklar / 19:00 Taom tortiladi / 19:45 Birinchi raqs / 20:15 Viktorina va o'yinlar / 21:00 Vals / 21:30 Raqslar / 22:30 Kelin-kuyovni kuzatish. Vaqtlar + sarlavhalar 4 til (kaa/uz/ru/en) da foydalanuvchi spec i bilan 100% mos.
- **Fayllar**: `src/pages/Index.tsx` (TIMELINE array + `LiveToySection` rebuild), `src/index.css` (`premium-timeline*` block to'liq almashtirildi), `src/i18n/locales/{kaa,uz,ru,en}.json` (`home.timeline` + `events` object, 9 key).
- **Layout**: Desktop = horizontal editorial timeline, `overflow-x:auto` + scroll-snap, ichki grid 2648px (min-width:max-content) lekin scroll container 1312px da clip qiladi → premium horizontal scroll, page-level h-scroll YO'Q. Gold connector line = `.premium-tl-node::before` horizontal gradient (champagne gold rgb(197,154,82)). 9 ta circular marker. Mobile (<=900px) = vertical grid (2.4rem | 1fr), gold line vertical, no h-scroll.
- **Animation**: IntersectionObserver `in-view` → opacity 0->1 + translateY(24px->0), 0.7s, stagger inline `transitionDelay` (i*0.07s). `prefers-reduced-motion:reduce` → opacity:1!important, transform:none (visible by default). Hover: img `scale(1.03)` + `brightness(1.06)`, 0.5s, no rotation.
- **Images**: ImageGen tool YO'Q (faqat VideoGen). Unsplash CDN id lari shu muhitda hal qilinmadi (slug ≠ CDN id, barchasi 404). Shuning uchun `loremflickr.com/900/1200/wedding?lock=21..29` themed placeholder ishlatildi (9/9 load OK, HTTP 200). Swappable: foydalanuvchi `TIMELINE` array ni real photo URL lar bilan almashtirishi kifoya (copy i18n da).
- **Verify** (`scripts/verify-timeline.mjs` — yangi, saqlanadi): Playwright chromium. Desktop 1440: 9 steps, 9 markers, in-view=true, minOpacity 0.96 (last step hali stagger jarayonda)→1, goldLine gradient, imgLoaded 9/9, pageHScroll=0 (timeline clip qilingan; qolgan ~5-11px hero video dan, pre-existing, overflow:hidden ichida, TEgmagan). Mobile 390: verticalStacked=true, overflow visible, pageHScroll=0. i18n /en /ru /uz: titles mos, html lang to'g'ri. reduced-motion: opacity 1 without scroll.
- **Vite eslatma**: dev server safe-delete shimi tufayli ishga tushmaydi; verify uchun `env -u CODEBUDDY_SESSION_ID -u CLAUDE_SESSION_ID -u NODE_OPTIONS npm run build` keyin `npm run preview -- --port 5179` ishlatildi.
- `tsc --noEmit` EXIT=0.

## Turn 17: 18:00 timeline image — guests arriving (real photo)
- Foydalanuvchi real wedding rasmni o'z izlab qo'shishni buyurdi (loremflickr placeholder emas). 9 ta rasm dan FAQAT 1-sini (arrival) almashtirish, layout/hero/boshqa section larga tegmaslik.
- **Manba**: Pexels (Pexels License — bepul, tijoratga ruxsat). ImageGen tool YO'Q, shuning uchun WebFetch orqali pexels.com/photo/... sahifasidan to'g'ri CDN URL olindi (Unsplash sahifasi JS-rendered, foydasiz).
- **5 ta nomzodni vizual tekshirish** (Read tool multimodal — model rasmni ko'radi): c1 couple+car red carpet / c2 woman foil curtain / c3 indian wedding umbrellas / c4 editorial guests gathering luxury venue + parasol / c5 bridesmaids stone building. **Tanlov: c4 (Pexels 32073435)** — aniq "guests arriving": erkaklar kostyum, ayollar coral/sage liboslar, pushti soyabon, sarv daraxtlar, hashamatli venue. Editorial, vertikal 2:3, 1200x1800, watermark/text yo'q, bride-portrait emas.
- **Download**: `https://images.pexels.com/photos/32073435/pexels-photo-32073435.jpeg?auto=compress&cs=tinysrgb&w=1200` → `public/timeline-guests.jpg` (365 KB, 1200x1800 JPEG). Vite `public/` → rootga serve qiladi → `<img src="/timeline-guests.jpg" />`.
- **O'zgartirish**: `src/pages/Index.tsx` da TIMELINE[0].img = `"/timeline-guests.jpg"` (faqat shu qator). Qolgan 8 ta loremflickr URL o'zgarmadi.
- **Verify**: tsc OK, vite build OK (dist/timeline-guests.jpg 365 KB), vite preview HTTP 200, asset HEAD Content-Length:365363. Playwright: step[0] src=localhost:5179/timeline-guests.jpg naturalWidth=1200 naturalHeight=1800 complete=true; step[1..8] src.host=loremflickr.com naturalW=900 (o'zgarmagan). Birinchi step kartasini screenshot qilib ko'rildi — crop toza, markazlangan, "guests arriving" ni aniq ko'rsatadi.
- Eslatma: Pexels dan rasm olganda `w=1600` parametr ba'zan 0 byte qaytaradi (transient), `w=1200` ishonchli va yetarli.

## Turn 18: GALLERY section — to'liq qayta ishlash (curated wedding-day story) (BAJARILDI)
- Foydalanuvchi eski 4 ta random Unsplash rasmni (1 katta + stol + bo'sh kreslolar + B&W juftlik) bitta to'y-kunining "curated visual story" ga aylantirishni so'radi. HAMMASI o'zim izlab tanlandi (user rasm bermadi). Faqat Gallery section; Hero/Header/Timeline/Pricing/Footer/Music/Routing/Language TEgmangan.
- **5 ta Pexels rasm** (`w=1200` bilan yuklab olindi, barchasi valid JPEG): `public/gallery-1.jpg` (bride+groom, 1200x1800), `gallery-2.jpg` (rings+florals, 1200x1800), `gallery-3.jpg` (ceremony people, 1200x1800), `gallery-4.jpg` (emotional embrace, 1200x1800), `gallery-5.jpg` (first dance, 1200x800). Pexels License — bepul, tijoratga ruxsat, attribution shart emas.
- **i18n**: 4 locale (`kaa/uz/ru/en`) `home.gallery` ga `captions` object qo'shildi: hero/details/ceremony/emotion/dance. `t(\`home.gallery.captions.${key}\`)` — dotted path `LanguageContext.lookup` da resolve bo'ladi (tasdiqlandi).
- **JSX** (`src/pages/Index.tsx`): `SEZIMLERI_GALLERY` array → 5 ta local `{src,cls,capKey,eager}`; `GallerySection` render endi `<figure class="gallery-tile gallery-tile--X">` + `<img>` (loading eager/faqat hero, qolgan lazy; `width/height` reserve; `alt` + `<figcaption>` i18n caption) + hover-da chiqadigan subtitle. Eski hardcoded "Bekzod/Gúljan" ismlari o'chirildi.
- **CSS** (`src/index.css`): `.gallery-editorial` block to'liq almashtirildi. Desktop (≥760px) asymmetric grid: hero span 2 rows (dominant, chapda) + details/ceremony o'ngda stacked + emotion chap-past + dance o'ng-past. Mobile: 1fr 1fr → hero full (1/-1), details+ceremony 2-up (yonma-yon), emotion full, dance full — aniq "1 large → 2 smaller → 1 wide → final" flow. Hover: `scale(1.03)` + `brightness(1.05)` 0.65s, no rotation/bounce. `prefers-reduced-motion` → no scale.
  - **MUHIM FIX**: mavjud `@media (max-width:767px)` bloki `.gallery-editorial { grid-template-columns: 1fr }` deb BARCHA mobile ni 1-column qilib qo'ygan edi (shuning uchun details/ceremony yonma-yon emas, full-width chiqardi). `1fr` → `1fr 1fr` ga o'zgartirildi (faqat gallery, boshqa section larga tegmadi).
- **Verify** (`scripts/verify-gallery.mjs` — yangi, saqlanadi, Playwright chromium): **18/18 PASS** — 5 tiles, hammasi local `/gallery-N.jpg`, decode OK, hero dominant (498>240), captions non-empty, hero eager + qolgan lazy, gallery section viewport ichida (h-overflow yo'q), 0 console/page error, hover `matrix(1.02994,...)` (scale 1.03), i18n en/ru/kaa/uz captions to'g'ri, mobile [350,167,167,350,350] (2-up works), mobile 0 h-scroll, reduced-motion hover none. `tsc --noEmit` EXIT=0, `vite build` OK.
- **Kuzatilgan pre-existing**: `opening.mp4` full-screen intro splash (aria "Open Vowly invitation") butun sahifa ustida `position:absolute` + `pointer-events:auto` bilan turibdi va Playwright `hover()` actionability ni block qiladi (headless da auto-dismiss bo'lmaydi). Gallery ga tegishli emas (Hero/Opening tashqarisida). Verify da splash `display:none` qilib neutralize qilindi (test-only). Real foydalanuvchi splash ni dismiss qilgach gallery ko'rinadi.
- **Kuzatilgan pre-existing #2**: Desktop da ~53px page-level horizontal scroll — `document.documentElement` (`html`) `overflow-x:visible` (Turn 7 `html,body{overflow-x:hidden}` qo'shgan lekin html ga apply bo'lmagan/override). Sabab = timeline `.premium-tl-grid` (2712px) — Gallery ga tegishli emas, out-of-scope. Gallery section O'ZI viewport ichida (galOverflow right=1440). Agar kerak bo'lsa `html{overflow-x:hidden}` global qo'shish mumkin (atimayapti, chunki user boshqa section larni o'zgartirmaslik aytdi).
- **Vite eslatma**: dev server safe-delete shimi tufayli ishga tushmaydi; verify uchun `env -u CODEBUDDY_SESSION_ID -u CLAUDE_SESSION_ID -u NODE_OPTIONS npm run build` keyin `npm run preview -- --port 5179` ishlatildi.

## Vowly: Admin/Super-Admin login white-screen fix (root cause)
- BUG: After successful auth, /admin and /super-admin sometimes rendered a completely blank WHITE screen.
- ROOT CAUSE (two reinforcing issues, both fixed):
  1. `useAuth` was a plain per-component hook (NO Context/Provider). Every consumer (LoginPage, ProtectedRoute, each dashboard) created its OWN Supabase `onAuthStateChange` subscription + `getSession()` + its own copy of auth state, all resolving asynchronously/independently → intermittent races/disagreement on `user`/`role`/`loading`.
  2. `onAuthStateChange` result was destructured as `const { data: { subscription } }` — would throw if `data` were null, and there was NO Error Boundary anywhere in `src/`. Any render/async error unmounted the entire React tree → silent blank white screen.
- FIX:
  - New `src/auth/AuthProvider.tsx`: single source of truth. ONE subscription, ONE resolution pass. `try/catch` around role/profile/hall queries; `role` always a concrete value; no-clobber logic so a transient null session can't wrongly redirect to 404; optional chaining on the subscription (no destructure throw).
  - `src/hooks/useAuth.ts` now just re-exports `useAuth`/`AuthProvider`/`AppRole` from the new module — all existing imports unchanged.
  - New `src/components/common/ErrorBoundary.tsx`: class boundary that RENDERS the real error + stack + reload button (surfaces the error, does NOT hide it). Wired in `main.tsx` around `<App/>`.
  - `App.tsx`: wrapped routes in `<AuthProvider>` inside `<LanguageProvider>`.
- VERIFY: `tsc --noEmit` OK; `npm run build` OK; Playwright smoke (port 5184) → landing/login render, unauth /ru/super-admin & /ru/admin correctly redirect to /ru/404 (renders 404 page, NOT blank); PAGE ERRORS []. Note: full post-login dashboard render not runtime-verified (no real admin creds in sandbox), but structural failure mode eliminated.

## SuperAdminDashboard dynamic-import 500 — ROOT CAUSE FOUND & FIXED
- BUG: /kaa/super-admin gave "Failed to fetch dynamically imported module: .../SuperAdminDashboard.tsx" (dark ErrorBoundary screen).
- ACTUAL ROOT CAUSE: `src/integrations/supabase/types.ts` (auto-generated Supabase types) returned HTTP 500 from the Vite DEV server. SWC (`@vitejs/plugin-react-swc` / `@swc/core` 1.13.2) could NOT parse the file, so the whole import chain `client.ts → SuperAdminDashboard.tsx` failed → dynamic import 500.
  - The file had a CORRUPTED orphaned fragment (lines ~276-288): a duplicated subset of `invitations` Insert fields + TWO stray `}` braces that prematurely closed `Tables` (depth 3→2) then `public` (depth 2→1). After that, `Views`/`Functions`/`Enums`/`CompositeTypes` over-closed, leaving final depth -2.
  - SWC first failed at `Views: { [_ in never]: never }` (mapped type it can't parse) → edited to `[key: string]: never`; but the deeper brace imbalance was the real issue.
- FIX: deleted the orphaned fragment (lines 276-288) so `Tables: {` stays open and contains all table entries; `Views`/`CompositeTypes` kept as `[key: string]: never` (valid TS index sig, SWC-parseable). Verified with a brace-trace script (final depth 0).
- Verified: `curl localhost:8080/src/integrations/supabase/types.ts` → HTTP 200 (was 500); `client.ts` + `SuperAdminDashboard.tsx` → 200; dev build transforms 2231 modules OK. Routing itself was never the cause (kaa is a valid locale, route registered correctly, `:locale` param doesn't enter the module path). `lazyWithRetry` added earlier in App.tsx is a harmless robustness improvement (kept).

## Guest invitation `:hallId` → Supabase 400 — ROOT CAUSE FOUND & FIXED (CURRENT)
- BUG: Deployed Guest invitation made `GET .../wedding_halls?select=*&id=eq.%3AhallId` → 400; page showed "Toyxana tabılmadı". Same for bride_groom/artists/food_items/timeline_events/banners.
- ACTUAL ROOT CAUSE: `src/App.tsx` `LegacyRedirect` helper did `to.replace(":slug", params.slug ?? "")` — but the hall route uses `:hallId`, NOT `:slug`. So when a QR (non-localized `/hall/<realId>?table=N`, generated by `QRCodeGenerator.tsx`) hit the legacy route `/hall/:hallId`, the redirect kept the literal `:hallId` placeholder → `/<locale>/hall/:hallId` → `GuestPage` `useParams()` returned the string ":hallId" → query `id=eq.:hallId`.
  - `GuestPage.tsx`/`useHallData.ts` themselves were CORRECT (real hallId from `useAuth()`, proper `useParams()`, hooks already `enabled: !!hallId`).
- FIX (3 files):
  1. `src/App.tsx` LegacyRedirect: generic param substitution `to.replace(/:([a-zA-Z_][a-zA-Z0-9_]*)/g, (_, k) => params[k] ?? "")` — substitutes `:hallId` (and `:slug`) correctly. This fixes ALL already-printed/deployed QR codes.
  2. `src/components/halladmin/QRCodeGenerator.tsx`: `baseUrl = ${origin}/${locale}/hall/${hallId}` (localized) so NEW QR codes land directly on the correct-language route and never need the legacy redirect.
  3. `src/pages/GuestPage.tsx`: defence-in-depth `isValidHallId = !!hallId && !hallId.includes(':')`; main query `enabled: isValidHallId`; dependent hooks get `safeHallId` ('' when invalid, so they don't fire); early-return shows "Toyxana tabılmadı" when `!isValidHallId || !hall`.
- VERIFY (Playwright, port 8080): `/hall/TESTHALL123?table=1` → redirects to `/<locale>/hall/TESTHALL123?table=1` (real id, table preserved); `/uz/hall/REALID999?table=7` → stays correct; actual Supabase request observed = `.../wedding_halls?select=*&id=eq.TESTHALL123` (REAL id, NOT `:hallId`); `bad(:hallId) requests = 0` in both flows. No new Supabase project, no fake data, no RLS disabled, no hardcoded id.

## Turn 19 — Invitation MUSIC upload + playback COMPLETELY FIXED (BAJARILDI)
- Foydalanuvchi: "VOWLY — FIX INVITATION MUSIC UPLOAD + PLAYBACK COMPLETELY". Musiqa tanlanib, taklifnomaga ulanmaydi va ochilganda chalimaydi edi. 18 bandli talab + 20 qadamli final test.
- **To'rt root cause (RC1-4)**:
  - RC1 (ASOSIY): `useCreateInvitation` payload `music_url` ni UMUMAN yozmagandi → yuklangan URL tashlab yuborilardi (faqat IndexedDB/localStorage fallback).
  - RC2: `invitations` jadvalida `music_url` column YO'Q edi.
  - RC3: `hall-assets` bucket ga `invitations/` prefix da anon INSERT/DELETE policy YO'Q edi → yuklash 400/RLS bloklanardi.
  - RC4: `InvitationPage` autoplay ni jimgina yutib yuborardi, musiqa control UI yo'q edi (play/pause/mute/equalizer yo'q, xato handling yo'q).
- **Migration** (`supabase/migrations/20260815000002_taklifnoma_music.sql` — YANGI): `ALTER TABLE public.invitations ADD COLUMN IF NOT EXISTS music_url text NULL;` + storage.objects INSERT+DELETE policy (`Anyone can upload/delete invitation music`, `anon, authenticated`, `bucket_id='hall-assets' AND (storage.foldername(name))[1]='invitations'`) — faqat `invitations/` folder ga scoped (hall-admin/wedding-photo ga tegmaydi), yangi bucket YO'Q (mavjud `hall-assets` public reuse). **USER action**: migration ni Supabase ga push qilishi kerak (`supabase db push` yoki Dashboard SQL Editor) — bu workspace da supabase CLI yo'q + auth yo'q.
- **Code o'zgarishlari**:
  - `src/integrations/supabase/types.ts`: `music_url: string | null` Row/Insert/Update ga qo'shildi (alphabetical: maps_url dan keyin).
  - `src/hooks/useInvitations.ts`: (RC1) payload ga `music_url: musicUrl ?? null`; upload MUZIKDAN OLDIN bo'ladi; (RC2 defensif) agar remote da column yo'q bo'lsa (`/music_url/` xato) → `music_url` ni olib retry qiladi (insert muvaffaqiyatli). Qo'shildi: `uploadMusicFile` `[MUSIC]` log + `musicPathFromUrl`/`deleteMusicFile` yordamchilari + `useUpdateInvitationMusic(slug)` mutation (replace/remove — eski URL ni o'chiradi). `useInvitation` merge endi type-safe (`data.music_url ?? extras.musicUrl`).
  - `src/pages/InvitationPage.tsx` (RC4, QAYTA YOZILDI): bitta doimiy `<audio>` element (loop, preload, per-render/section qayta yaratilmaydi); `MusicController` (play/pause, mute/unmute, animated equalizer `.inv-eq.is-on`); autoplay try → bloklansa `armUnlock` (keyingi pointerdown/touchstart da resume); `handleAudioError` → DB url 404/403 bo'lsa local IndexedDB ga fallback; `localStorage` `vowly:music-pref:<slug>` da mute/play saqlanadi (refresh/reopen da tiklanadi); `type="button"` barcha control larda; `[MUSIC]` log lar.
  - `src/components/invitation/builder/forms/index.tsx` (MessageForm): `musicType` validatsiyasi (MP3/WAV/M4A/AAC/OGG + `file.type.startsWith("audio/")`), 12MB limit, local preview `<audio controls>`, filename + `formatBytes(size)`, replace (`RotateCw`) + remove (`X`), `type="button"`.
  - `src/i18n/locales/{kaa,uz,ru,en}.json`: yangi `builder.message.musicType / musicReplace / musicStatus`.
  - `src/index.css`: `.inv-music-size`, `.inv-music-preview`, `.inv-music-ctrl` (fixed pill), `.inv-music-btn`, `.inv-eq` + `.inv-eq-bar` + `@keyframes invEq`.
- **VERIFY** (`e2e_music.mjs` — Playwright chromium, vite preview port 8099): **BARCHASI PASS** — builder 6 step to'ldirish → musiqa tanlash (WAV fixture, validation OK) → chip ko'rinadi → create → published `/taklifnoma/<slug>` → controller render → video fallback (gesture) orqali musiqa chalindi (`.inv-eq.is-on`, `audio.paused=false`, src=`blob:`) → mute toggle ishlaydi. `[MUSIC]` log lar RC1-3 ni TASDIQLADI: `uploading music .../invitations/...wav` → `upload failed ... violates row-level security policy` (RC3 — migration qo'llanmagan) → `music_url column missing on remote, retrying without it` (RC2) → `invitation created, music_url=none` → `playback started -> blob:`. Ya'ni kod to'g'ri, faqat cross-device persistence uchun migration kerak. `vite build` exit 0; `tsc --noEmit` (bizning fayllarimizda 0 xato; pre-existing 4 ta xato AuthProvider/templates/idb/InvitationBuilder da, bizga tegishli emas). Temp test fayli o'chirildi.
- **Qoldiq QADAQ**: migration remote ga push qilinganda (user tomonidan) → `music_url` column + anon storage policy ishga tushadi → upload muvaffaqiyatli, DB URL persist bo'ladi, viewer DB URL ni afzal ko'radi → QR/cross-device musiqa ishlaydi. Hozirgi local IndexedDB fallback faqat creating device da ishlaydi.
- **Eslatma**: vite dev server `/node_modules/.vite` bulk-delete safe-delete shimi tufayli ishga tushmaydi; verify uchun `vite preview` (build qilingan dist) ishlatildi.

## Turn 20 — Landing MOBILE responsive yakunlandi (12-bandli talab, BAJARILDI)
Foydalanuvchi 12 bandli professional mobile responsive talab berdi (h-scroll YO'Q, timeline TIME↓IMG↓TITLE↓DESC, gold rail, img width:100%, heading white-space:normal+clamp, CTA responsive width:100%, 20-24px gutter, hero premium, header bir qator, breakpoints 320-430+768, desktop BUZILMASIN).

**Yakuniy natija**: 8 ta width (320/360/375/390/414/430/768/1280) `scrollX=0`, `offenders=0`, timeline stacked, desktop o'zgarmagan. `mobile-qa.mjs` `sx<=0 && offenders==0` kriteriyasi bilan ALL CHECKS PASSED.

**Muhim o'zgartirishlar** (`src/index.css`):
1. **Global guards**: `html{overflow-x:clip}` + `img,video,svg,canvas{max-width:100%;height:auto}`; text-overflow guard `.premium-heading, .premium-sub, ...` ga `white-space:normal; overflow-wrap:break-word; word-break:break-word`.
2. **Timeline `@media (max-width:900px)` qayta yozildi** (vertikal gold rail, minimal dot): `.premium-tl-step` `display:grid` `grid-template-areas:"rail time" "rail img" "rail title" "rail desc"`, `grid-template-columns: 2.2rem minmax(0,1fr)` (minmax(0,1fr) — uzun so'zlarni bir qatorda ushlatmaslik uchun), `.premium-tl-scroll{overflow:visible}`. TIME→IMAGE→TITLE→DESC aniq tartibda, har biri alohida qatorda.
3. **Premium headings** `@media (max-width:900px)`: `.premium-heading em{display:block; margin-top:.1em}` — gold aksent o'z qatoriga (QR/Features `<br/>` bilan mos, boshqa 4 heading uchun ham ikki qatorli premium layout). `.section-premium .vow-btn{max-width:100%}` — tablet 2-col headerlarda CTA tugmasi cell dan chiqib subtitle ustiga bosmasin.
4. **Hero** `@media (max-width:900px)`: `.hero-premium-title{line-height:1.2}` (default 1.04 juda tig'iz, descender/ascender optik collision); `.hero-title-accent{display:inline-block; margin-top:.12em}` (italic gold aksent pastga surildi).
5. **Mobile refinements** `@media (max-width:767px)`: `.vow-wrap-wide{padding-left:1.5rem; padding-right:1.5rem}` (24px gutter); `.hero-content{padding:0 1.25rem}`; `.hero-premium-title{font-size:clamp(2.1rem,9vw,3.4rem)}`; `.section-premium .vow-btn{width:100%; max-width:100%}` (footer newsletter submit scoped out); `.vow-btn{white-space:normal}`; `.pricing-card-cta{white-space:normal}`; `.gallery-editorial{grid-template-columns:1fr 1fr}`.
6. **Header ≤380px**: `.brand .logo-mark{height:22px}`, `.font-display{font-size:1.375rem}`, `.premium-burger{2.4rem}`, `.premium-lang-trigger{height:2.1rem; padding:0 0.6rem}`.
7. **Hero** `contain: paint` qo'shildi — zoomed hero video document scroll area kengaytirishini oldini oladi.

**Pricing heading collision (topilgan)**: `title1<em>title2</em>` inline (bo'sh joysiz), Cormorant Garamond `line-height:1.06` + serif descender/ascender → "тариф"+"выберите" optik ustma-ust. Em→block fix (375/430 OK), lekin 768 da ham kerak — `@media (max-width:900px)` ga ko'chirildi (767 alone yetarli emas edi).

**Timeline CTA overflow (topilgan 768)**: `md:grid-cols-2` 768 da 2-col, tugma (~380px) o'ng column (~344px) dan chapga overflow → subtitle 4-qator ustiga tushgan. `.section-premium .vow-btn{max-width:100%}` 900 block da → tugma cell ga sig'adi, 32px gap (overlap=false, computed `gap:32`).

**Hero accent collision (topilgan 320)**: `line-height:1.04` optik collision (italic ascender line1 descender ga tegadi). `line-height:1.2` YETARLI EMAS (computed gap 0 — line boxes adjacent). `.hero-title-accent{display:inline-block; margin-top:.12em}` → 5px real gap (line1Bottom:273 → spanTop:278). Crop screenshot `hero-title-320.png` da ikki qator ANIQ ajralgan.

**Scripts** (yangilari saqlanadi):
- `scripts/mobile-qa.mjs` — 8 width, `sx<=0 && offenders==0`, timeline stack order, screenshots `scripts/shots/ru-<w>.png`.
- `scripts/check-em.mjs` — `.premium-heading em` computed display (block@≤900, inline@≥1024).
- `scripts/check-btn.mjs` — timeline CTA overlap check (subRight vs btnLeft).
- `scripts/check-hero.mjs` — hero title line-height + accent rect/gap.
- `scripts/diag2.mjs`, `scripts/verify-fix.mjs`, `scripts/verify-desktop.mjs`, `scripts/shots2.mjs`, `scripts/hero-crop.mjs`.

**DEV SERVER MUHIM ESLATMA (turn 20 + previous turns bilan bog'liq)**:
- **safe-delete shim**: `genie-safe-delete.cjs` `NODE_OPTIONS="--require=..."` orqali yuklanadi, `rmSync`/`trash` ni fail-closed qiladi. `vite build --outDir <existing>` da `emptyDir` bloklanadi. **YECHIM**: `NODE_OPTIONS="" npx vite build --outDir dist-<fresh-name>` (yangi papka — `emptyDir` no-op). Yoki `NODE_OPTIONS=""` siz ham fresh papka ishlatilsa yetarli (vite build `rmSync` chaqirmaydi outDir yo'q bo'lsa).
- **`vite preview` port fallback**: agar port occupied bo'lsa, Vite SIZGA OGOHLANTIRMASDAN keyingi portga o'tadi ("Port 4180 is in use, trying another one... ➜ Local: http://localhost:4181/"). Bu — **BUG KABI** ko'rinadi: `curl localhost:4180` eski build ni qaytaradi, yangi CSS kelmaydi, QA natijasi noto'g'ri bo'ladi. **DOIM curl bilan tekshir**: `curl -s http://localhost:<port>/ | grep -oE 'href="[^"]*\.css"'` — hash yangi build dist-<name> bilan mos bo'lishi kerak. Turn 20 da 4180 occupied → 4181 ga tushdi, keyin 4182, 4183, 4184. Bu 30+ daqiqa vaqt yo'qotishga sabab bo'ldi. **MUAMMONI OLDINI OLISH**: har bir yangi build dan keyin `TaskOutput` da port ni ANIQ o'qi (`➜ Local: http://localhost:XXXX/`), yoki portni qo'lda unique tanla (`4184`, `5180`, etc.), yoki oldingi preview larni `TaskStop` qilib port bo'shat.
- Playwright chromium: `NODE_OPTIONS="" npx playwright install chromium` (shimsiz) — `%LOCALAPPDATA%/ms-playwright` ga o'rnatadi.
- `playwright` package `@playwright/test` dan resolved (hoisted) — `import { chromium } from "playwright"` ishlaydi.

**Desktop regression check**: 1280px da `.premium-heading em` inline (display=inline, confirmed), pricing card 3-col, timeline horizontal scroll (`.premium-tl-scroll{overflow-x:auto}` — by design, internal scroller), hero CTA side-by-side, nav to'liq ko'rinadi. Hech qanday regress.
