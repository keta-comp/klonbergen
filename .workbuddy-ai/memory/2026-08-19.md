# 2026-08-19

## Reconnecting Supabase to a NEW project (user: "Supebaseni qayta boshqasiga ulaymiz")
User is switching the Vowly app to a fresh Supabase project (klonbergen.vercel.app frontend). Findings + deliverables:

**Current config:**
- `src/integrations/supabase/client.ts` reads `import.meta.env.VITE_SUPABASE_URL` + `VITE_SUPABASE_PUBLISHABLE_KEY`.
- `.env` holds 3 vars: `VITE_SUPABASE_URL`, `VITE_SUPABASE_PUBLISHABLE_KEY`, `VITE_SUPABASE_PROJECT_ID`.
- `supabase/config.toml` had `project_id = "abdicwpyjvmnuevzytct"` (old project).
- **Security fix:** `.gitignore` was missing `.env` → added `.env`, `.env.*`, `!.env.example` so secrets are never committed.

**Key insight:** for a FRESH project the 25 migrations (in `supabase/migrations/`, chronological) already contain the COMPLETE schema — including the billing tables, ALL RPCs (`confirm_subscription_payment`, `sync_subscription_notifications`, `seed_default_plans`, `has_role`, `is_super_admin`, `today_in_tashkent`, `subscription_days_remaining`), the `wedding_halls` extensions, and the single 299 000 UZS 'venue' plan seed (edited earlier to one plan + `DELETE ... WHERE code <> 'venue'`). `install_billing.sql` is a duplicate of that billing portion and its `CREATE TYPE public.app_role` is NOT idempotent → would fail if concatenated.

**Deliverable:** `supabase/sql/00_consolidated_schema.sql` — all 25 migrations concatenated in order (no install_billing.sql), safe to run ONCE on a brand-new project. This is the single file to paste into the new project's SQL Editor and replaces the old "run install_billing.sql" guidance.

**Reconnection checklist given to user:**
1. Create new Supabase project; copy URL + anon key + project ref.
2. Update `.env` (3 vars) and `supabase/config.toml` `project_id`.
3. Run `00_consolidated_schema.sql` in the new project's SQL Editor (one shot).
4. Deploy Edge Functions (`supabase/functions/*`: create-hall-admin, archive-weddings, build-wedding-zip) — `create-hall-admin` is invoked by AddHallModal.
5. Update Vercel env vars (live app reads Vercel env, not local `.env`) and redeploy.

`.env.example` created as a safe template.

**Credentials applied (user pasted new project keys):**
- New project ref: `vbikhnzwnsfddgjzwuge`, URL `https://vbikhnzwnsfddgjzwuge.supabase.co`.
- User supplied keys under `NEXT_PUBLIC_` prefix, but app is Vite and `client.ts` reads `VITE_SUPABASE_URL` / `VITE_SUPABASE_PUBLISHABLE_KEY` → mapped to `VITE_` prefix in `.env`. Written `VITE_SUPABASE_URL`, `VITE_SUPABASE_PUBLISHABLE_KEY=sb_publishable_gXkv8OaGvXHwkHJwRibAtQ_SxACbUna`, `VITE_SUPABASE_PROJECT_ID=vbikhnzwnsfddgjzwuge`. Also updated `supabase/config.toml` `project_id`.
- Rebuilt `dist/`; confirmed new ref inlined in bundle. `tsc`/`vite build` green.
- **STILL REQUIRED by user:** (1) run `00_consolidated_schema.sql` in the NEW project's SQL Editor (DB is empty otherwise → all 404/400 return); (2) deploy Edge Functions (`create-hall-admin`, etc.); (3) set Vercel env vars to the VITE_ values and redeploy (live app reads Vercel env, not local .env).
