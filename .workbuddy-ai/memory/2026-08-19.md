# 2026-08-19

## Reconnecting Supabase to a NEW project (user: "Supebaseni qayta boshqasiga ulaymiz")
User is switching the Vowly app to a fresh Supabase project (klonbergen.vercel.app frontend). Findings + deliverables:

**Current config:**
- `src/integrations/supabase/client.ts` reads `import.meta.env.VITE_SUPABASE_URL` + `VITE_SUPABASE_PUBLISHABLE_KEY`.
- `.env` holds 3 vars: `VITE_SUPABASE_URL`, `VITE_SUPABASE_PUBLISHABLE_KEY`, `VITE_SUPABASE_PROJECT_ID`.
- `supabase/config.toml` had `project_id = "abdicwpyjvmnuevzytct"` (old project).
- **Security fix:** `.gitignore` was missing `.env` → added `.env`, `.env.*`, `!.env.example` so secrets are never committed.

**Key insight:** for a FRESH project the 25 migrations (in `supabase/migrations/`, chronological) already contain the COMPLETE schema — including the billing tables, ALL RPCs (`confirm_subscription_payment`, `sync_subscription_notifications`, `seed_default_plans`, `has_role`, `is_super_admin`, `today_in_tashkent`, `subscription_days_remaining`), the `wedding_halls` extensions, and the single 299 000 UZS 'venue' plan seed (edited earlier to one plan + `DELETE ... WHERE code <> 'venue'`). `install_billing.sql` is a duplicate of that billing portion and its `CREATE TYPE public.app_role` is NOT idempotent → would fail if concatenated.

**Deliverable:** `supabase/sql/00_consolidated_schema.sql` — all 25 migrations concatenated in order (no install_billing.sql), safe to run ONCE on a brand-new project. This is the single file to paste into the new project's SQL Editor and replaces the old "run install_billing.sql" guidance.

**Reconnection checklist given to user:**
1. Create new Supabase project; copy URL + anon key + project ref.
2. Update `.env` (3 vars) and `supabase/config.toml` `project_id`.
3. Run `00_consolidated_schema.sql` in the new project's SQL Editor (one shot).
4. Deploy Edge Functions (`supabase/functions/*`: create-hall-admin, archive-weddings, build-wedding-zip) — `create-hall-admin` is invoked by AddHallModal.
5. Update Vercel env vars (live app reads Vercel env, not local `.env`) and redeploy.

`.env.example` created as a safe template.

**Credentials applied (user pasted new project keys):**
- New project ref: `vbikhnzwnsfddgjzwuge`, URL `https://vbikhnzwnsfddgjzwuge.supabase.co`.
- User supplied keys under `NEXT_PUBLIC_` prefix, but app is Vite and `client.ts` reads `VITE_SUPABASE_URL` / `VITE_SUPABASE_PUBLISHABLE_KEY` → mapped to `VITE_` prefix in `.env`. Written `VITE_SUPABASE_URL`, `VITE_SUPABASE_PUBLISHABLE_KEY=sb_publishable_gXkv8OaGvXHwkHJwRibAtQ_SxACbUna`, `VITE_SUPABASE_PROJECT_ID=vbikhnzwnsfddgjzwuge`. Also updated `supabase/config.toml` `project_id`.
- Rebuilt `dist/`; confirmed new ref inlined in bundle. `tsc`/`vite build` green.
- **STILL REQUIRED by user:** (1) run `00_consolidated_schema.sql` in the NEW project's SQL Editor (DB is empty otherwise → all 404/400 return); (2) deploy Edge Functions (`create-hall-admin`, etc.); (3) set Vercel env vars to the VITE_ values and redeploy (live app reads Vercel env, not local .env).

## Login 400 `invalid_credentials` on live app — ROOT CAUSE found
User pasted a failed `POST /auth/v1/token?grant_type=password` from `https://klonbergen.vercel.app`:
- **Request URL + `apikey`/`authorization` JWT `ref` = `abdicwpyjvmnuevzytct`** → the LIVE Vercel app is STILL wired to the OLD project. The credential switch (client.ts `.env` config.toml → new `vbikhnzwnsfddgjzwuge`) was NEVER deployed to Vercel, so the live bundle still uses old env.
- `x-sb-error-code: invalid_credentials` = Supabase rejected the email/password against the OLD project (unknown email or wrong password there). NOT a config error.
- **Verified by curl:** NEW project `vbikhnzwnsfddgjzwuge` auth settings = 200 (key+URL valid/reachable), but `/rest/v1/wedding_halls` = 404 → its schema was NEVER deployed (confirms step 1 still outstanding). OLD project auth settings = 200.

**Conclusion:** the migration to the new project is incomplete on the DEPLOYMENT side. The live app must be redeployed with new env vars; until then login hits old project and fails.

**Ordered fix to make login work on the NEW project:**
1. Run `supabase/sql/00_consolidated_schema.sql` in NEW project SQL Editor (creates tables/RPCs/299k plan; fixes the 404).
2. In Vercel dashboard set env for `klonbergen`: `NEXT_PUBLIC_SUPABASE_URL=https://vbikhnzwnsfddgjzwuge.supabase.co`, `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=sb_publishable_gXkv8OaGvXHwkHJwRibAtQ_SxACbUna` (client.ts now accepts both prefixes + vite.config has envPrefix for both).
3. Commit & push the client.ts + vite.config.ts changes (Vercel builds from git, not local dist/; `.env` is gitignored so Vercel must get vars from its dashboard), then trigger deploy.
4. Create admin user in NEW project (signup via app), then grant superadmin via SQL: `INSERT INTO public.user_roles (user_id, role) VALUES ('<auth.users uuid>', 'super_admin');` (table confirmed in 00_consolidated_schema.sql; no UI for this yet). Without this, login succeeds but SuperAdmin dashboard is inaccessible.
5. Deploy Edge Functions (`create-hall-admin`, `archive-weddings`, `build-wedding-zip`) to NEW project.
6. Login with the NEW-project account.
NOTE: if the user tries to log in with the OLD account's email after redeploy, they'll get `invalid_credentials` again — that account lives in the OLD project, NEW project starts with zero users.

## Login 400 — UPDATE (user's 2nd dump, ~5 min later)
New dump now shows `:authority: vbikhnzwnsfddgjzwuge.supabase.co` + `apikey: sb_publishable_...` (new key) + `sb-project-ref: vbikhnzwnsfddgjzwuge`. **=> Deployment switch is CONFIRMED DONE.** Live app now targets new project.
- Re-probed new project: `auth/v1/settings` = 200 (good), but `/rest/v1/wedding_halls` = 404 and `/rest/v1/plans` = 404 → **schema STILL not deployed** (`00_consolidated_schema.sql` not run yet).
- `invalid_credentials` now = the email/password isn't in the NEW project's `auth.users` (empty). Expected — new project has no users; old account is in old project.
REMAINING to get logged in:
1. Run `00_consolidated_schema.sql` on new project (fixes 404s).
2. Create admin account in new project (Supabase dashboard → Authentication → Add user, auto-confirmed; or app signup).
3. Grant superadmin: `INSERT INTO public.user_roles (user_id, role) SELECT id,'super_admin'::app_role FROM auth.users WHERE email='<email>';`
4. Login. (Edge Functions deploy is non-blocking for login.)

## Prepared setup helper for user ("togirlab beraqol")
User asked me to just finish setup. Reality: schema DDL + superadmin grant require the Supabase DASHBOARD (service_role / DB password), which I cannot access — so I can't run them, only prepare them. Created `supabase/sql/01_create_superadmin.sql`: idempotent `DO $$` block that grants `super_admin` to either a specified email (`v_email`) or the most-recently-created user (ideal for a fresh project). Run order in dashboard: (1) `00_consolidated_schema.sql`, (2) create admin via Authentication → Add user, (3) `01_create_superadmin.sql`, (4) login. Code/config side fully done; only dashboard pastes remain.
