# 2026-08-14

## Landing sahifaga video qo'shildi (Vowly)
- `src/components/visual/OpeningExperience.tsx` (yangi) — to'liq ekran "mirátnama ashıw" kinocha intro; `/opening.mp4` bosilganda chalinaadi, tugagach sayt ochiladi; scroll-lock; `prefers-reduced-motion` da o'tkazib yuboriladi.
- `src/pages/Index.tsx` — HeroSection ga `/hero.mp4` fon videosi + `.hero-scrim` qo'shildi; `<OpeningExperience />` landingda render qilinadi.
- `src/index.css` — `.hero-video`, `.hero-scrim`, `@keyframes slowzoom` qo'shildi; reduced-motion da slowzoom o'chiriladi.
- Video fayllarini foydalanuvchi `public/opening.mp4` va `public/hero.mp4` nomi bilan qo'yishi kerak.
- Tekshirish: `npm run build` + `tsc --noEmit` xatasiz o'tdi. `node_modules` bun orqali o'rnatildi (ilgari yo'q edi).

## OpeningExperience scroll-lock tuzatildi (final, ishlaydigan yechim)
- Muammo: overlay qo'lda DOM dan o'chirilgani (`remove()`) yoki komponent `return null` qaytargani uchun React unmount bo'lmas, `useEffect` cleanup hech qachon ishlamas va `document.body.style.overflow="hidden"` abadiy qolardi — pastdagi kontent ko'rinmas/scroll bo'lmas edi (foydalanuvchi 2 marta xabar qildi).
- Yechim (final): `prevOverflowRef` da eski overflow qiymatini saqlab, `reveal()` ichidagi `setTimeout` da aniq `unlock()` chaqiriladi (`document.body.style.overflow = prevOverflowRef.current`), keyin `setMounted(false)`. Bu cleanup ga bog'liq emas, shuning uchun ishlaydi.

## Premium editorial qayta dizayn yakunlandi (23-band art-direction brief)
- `src/index.css` ga to'liq editorial design-system qo'shildi: ivory/charcoal/champagne-gold palitra, Cormorant Garamond + Inter tokenlari, `.vow-*` utility klasslar (section/wrap/display/label/btn/cursor/grain/masonry/timeline/stat), reduced-motion override.
- `src/pages/Index.tsx` to'liq qayta yozildi: LandingNav, HeroSection (`/hero.mp4` SAQLANDI), NegeSection, QrExperienceSection, GuestExperienceSection, LiveTimelineSection, GallerySection (masonry), VenueSection, OnePlatformSection, PricingSection, RevenueSection (CountUp), TrustSection, FinalCtaSection, Footer.
- `src/components/visual/CustomCursor.tsx` (yangi) — desktop custom cursor, `data-cursor` VIEW/OPEN/EXPLORE.
- `src/components/visual/FilmGrain.tsx` (yangi) — `.film-grain` overlay.
- Hero videosi (`/hero.mp4` = kelin-kuyov birga yurish) va opening tajribasi (`/opening.mp4`) o'zgartirilmadi — saqlandi.
- Tekshirish: `bunx tsc -p tsconfig.app.json --noEmit` → TSC_EXIT:0; Vite barcha yangi modullarni HTTP 200 bilan transform qiladi; dev server `http://localhost:8080/` → HTTP 200 + to'g'ri `<title>`. Production build fon rejimda (sekin FS) ishlayapti.

## VIZUAL TAKRORLANISH tuzatildi (asset deduplication)
- Muammo: bir xil Unsplash rasmi bir necha sektsiyada qayta ishlatilgan (`1519741497674` 4x, `1511285560929` 4x, `1519225421980` 3x) — sayt "AI-generatsiya" kabi takrorlanuvchan ko'rinardi. Shuningdek 2 ta eski rasm (`1465498545567-1cce4db3672f`, `1606800052052-a08afd965338`) 404 edi.
- Yechim (`src/pages/Index.tsx`):
  - NegeSection dagi to'y stoli rasmi o'chirildi → yangi `InvitationCard` mahsulot-UI komponenti (CSS/SVG faux-QR, "Aygúl & Marat", stol №7). Rasm yo'q.
  - GuestScreen "galereya" tab: 3 ta dubl' bazasi o'rniga 3 ta noyob ID (`1520854221256`, `1521119989659`, `1494955870715`).
  - GallerySection: 6 ta to'liq noyob, tekshirilgan (HTTP 200) rasmga almashtirildi (eski 2 ta 404 ham tuzatildi).
  - FinalCtaSection foni: `1519741497674` o'rniga noyob `1591608971362`.
- Barcha Unsplash ID lari curl orqali GET tekshirildi (HEAD Unsplash da bloklanadi, faqat GET ishlaydi). Yakuniy xarita: HERO=/hero.mp4; Nege=InvitationCard; QR=phone UI; Guest=3 noyob; Live=timeline; Gallery=6 noyob; Venue=dashboard; Final=1 noyob. Hech qaysi rasm ikki katta sektsiyada takrorlanmaydi.
- Tekshirish: `tsc --noEmit` TSC_EXIT:0; `grep` bilan barcha eski ID lar o'chirilgani va 10 ta yangi ID ning har biri aniq 1 marta uchrashi tasdiqlandi; Vite Index.tsx ni HTTP 200 bilan transform qiladi.

## Premium qayta dizayn YAKUNIY TEKSHIRUVI (responsive + funksionallik)
- `tsc --noEmit` → 0 xato (toza). Dev server `http://localhost:8085/`.
- Playwright bilan 6 breakpoint (375/390/414/768/1024/1440): hamma joyda gorizontal scroll YO'Q (`scrollWidth===innerWidth`, offenders=0). Hero `<video>` atayin viewport dan tashqariga chiqadi (full-bleed) lekin `.hero{overflow:hidden}` bilan kesiladi — false positive.
- Layout ustunlari: mobile 1/1/1, tablet 2/2/1, desktop 3/3/5 (feature/pricing/timeline). Nav: `<1024px` da inline linklar yashiringan (`hidden lg:flex` nav), burger ko'rinadi; `>=1024px` da aksincha.
- Asset integriteti: `hero.mp4` autoplay (1280x720, paused=false, default motion da ham chalinaadi); `5.png` (1190x1322) va `6.png` (1156x1360) to'liq yuklanadi.
- Haqiqiy text-clipping (ancestor `overflow:hidden` bilan kesilish): 375/768/1440 da 0 ta.
- Funksionallik smoke: mobile menyu ochiladi (5 link), anchor scroll `#qalay`/`#bahalar` sektsiyani tepaga olib keladi, "Demo soraw" → `/login`. Page/console xatosi YO'Q.
- OpeningExperience envelope gate: default motion da faol (`z-9999`, klik talab qiladi) — BU TO'G'RI (designed). O'lchash uchun `reducedMotion:"reduce"` context ishlatildi (gate `null` qaytaradi).
- Skriptlar: `scripts/verify-premium.mjs` (overflow+asset), `scripts/verify-quality.mjs` (clip/nav/layout), `scripts/verify-clip.mjs` (ancestor-clip), `scripts/verify-functional.mjs` + `scripts/verify-video-nav.mjs` (interaction+video).
- QOLDIRILGAN: musiqa yuklash → 1-qadamga qaytish bug'i (oldingi sessiyadan) hali tasdiqlanmadi — foydalanuvchi aniqlik kiritishi kutilmoqda.
